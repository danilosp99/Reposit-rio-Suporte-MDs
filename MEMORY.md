# MEMORY.md — Registro de Decisões

> Atualize este arquivo ao longo do projeto com decisões, descobertas e contexto relevante.

## Decisões Técnicas
- (02/09/2026) ⚠️ Workflow de avaliação adversarial (3 personas de diretor atacando o material + defesas por categoria) FALHOU — os 3 agentes de ataque bateram em "session limit" (reset 11:50 America/Sao_Paulo) antes de rodar, resultado veio vazio. Não esperei o reset (usuário estava no chat) — fiz a avaliação eu mesmo, direto, sem subagentes, com as mesmas 3 lentes (Diretora de Risco/Modelagem, Diretor Comercial/Financeiro, Diretor de Operações/Governança), 5 ataques por persona + veredito honesto (defendido/parcialmente defendido/gap real) pra cada. Achado mais importante do exercício: o modelo **P7** (o que hoje decide a Lista de Pré-Aprovados, ANTES do funil) nunca entrou em nenhuma comparação deste projeto — só ASA V5 x ASA BVS — apesar da coluna `bvs_score_p7` já existir em `df_super_base_funil` (`config.COLUNAS_SUPER_BASE_ASA_BVS`), sem uso em nenhum cálculo. É o gap mais grave E o mais barato de fechar (extensão de código já pronto, sem nova extração). Contexto de negócio que o responsável deu e que embasa esse capítulo: hoje a Lista usa P7 pra entrada, mas a esteira de crédito decide com ASA V5 (multi-bureau, caro de escorar todo mundo) — a proposta do ASA BVS (mono-bureau BVS, barato) é reduzir essa disparidade P7-vs-V5, não só "ser melhor que o V5" isoladamente.
- (02/09/2026) A pedido do responsável ("SIM"), virou capítulo formal — **13 "Perguntas e objeções esperadas"** no HTML (13.1-13.3 = 1 seção por persona, 5 ataques cada, com `<div class="note crit/warn/info">` — usei a classe `.note.crit` que já existia no CSS mas nunca tinha sido usada no documento, perfeita pro veredito "gap real"; 13.4 = síntese com os 5 gaps prioritários em tabela) + **5 slides de anexo/backup no PPTX** (capa do anexo, 1 slide por persona com `add_linha_ataque()` — novo helper, tag colorida GAP REAL/PARCIAL/DEFENDIDO + título + defesa curta — e a síntese final), deck foi de 17 pra 22 slides. Nova cor `VERMELHO` (`#D03B3B`, mesmo tom do `--critical` do CSS do HTML) adicionada ao `build_pptx.py` pra manter a paleta de veredito consistente entre os 2 documentos. Tag balance do HTML revalidado (h2:13/section:13, tudo 1:1) e 89 `href="#"` conferidos sem nenhum quebrado, sem id duplicado.
- (02/09/2026) Pedido do responsável, olhando os slides já com dado real: as visões de "mix de rating" (12.3/slide 5) e "badrate por rating" (12.5/slide 7) mostravam só o CONSOLIDADO (média/agregado dos meses) — pediu a visão POR ANOMES em vez de consolidada, pra comparar V5 x BVS mês a mês. Nova `visualizacao.plot_grade_rating_v5_bvs(tabela_v5, tabela_bvs, titulo)` (grade de miniaturas, 1 subplot por rating — até 4 colunas — 2 linhas por painel, mesma paleta azul/verde do 2.3.6/2.3.10) + nova `### 2.3.12` no notebook (entre 2.3.11 e o Bloco 3, ids `93f65c5e`/`185e73cb`) gerando 5 figuras: `grade_mix_rating.png` (reaproveita `mix_rating_v5`/`mix_rating_bvs` do 2.3.11) + `grade_badrate_{over30m4,over60m6,fpd15,fpd30}.png` (RECALCULA `tabela_rating_v5`/`bvs` por indicador, já que o loop do 2.3.7/2.3.8 sobrescreve essas variáveis a cada iteração — só a última sobrevive no namespace, não dava pra reaproveitar direto). Confirmei visualmente (li os PNGs) que os gráficos saíram corretos — inclusive dá pra VER a safra imatura zerando nos meses recentes de cada indicador, reforçando visualmente o corte de "meses maduros" já usado nas tabelas agregadas. HTML: seção 12.3 trocou a tabela consolidada pelo gráfico como conteúdo PRINCIPAL (mantive a leitura do A2 e a nota de comparação com a migração, agora referenciando os painéis do gráfico); seção 12.5 GANHOU os 4 gráficos (grid 2x2 de cards) ANTES da tabela agregada, que virou "detalhe complementar" em vez de único conteúdo — removida a nota antiga "por que não tem gráfico aqui" (ficou obsoleta). PPTX: slide 5 (mix) trocou tabela por imagem única grande; slide 7 (badrate) virou um slide-resumo (mantém a tabela agregada + nota) seguido de 4 slides NOVOS dedicados (1 imagem grande cada, Over30M4/60M6/FPD15/30) — deck foi de 13 pra 17 slides. Também corrigido en passant: a capa (slide 1) ainda dizia "estrutura pronta — números pendentes de rerun", desatualizado desde que os números reais entraram — trocado pra "dado real, uma rodada única do notebook (02/09/2026)".
- (02/09/2026) ✅ ENTREGA EXECUTIVA COM DADO REAL — o responsável rodou o notebook do zero (kernel limpo, com o bug do FPD e o cache já corrigidos) e pediu pra preencher HTML + PPTX com os números reais. Extraí os outputs direto do `.ipynb` (parseando `text/html` das tabelas estilizadas via script Node, e LENDO as imagens PNG direto — migração de rating e curvas KS — já que o valor da diagonal só existe no gráfico, não em texto) + usei Python pra agregar badrate por rating SÓ nos meses maduros de cada indicador (soma bruta primeiro, divide depois — nunca média das % mensais, mesma regra do projeto) a partir das tabelas de contagem absoluta (mais confiável que re-derivar de %). Achados principais (todos com número real agora, capítulo 12 do HTML e os 13 slides do PPTX): (1) cobertura 63,7% ambos/21,7% só V5/12,9% só BVS/1,8% nenhum, base sólida; (2) migração: diagonal 28-48% na maioria das faixas, EXCETO A2 (1,7%) — achado forte, A2 praticamente some no BVS; (3) mix por mês confirma o mesmo: A2 cai de 18,7% (V5) pra 0,6% (BVS) na média, redistribuído pra AA (+4,8pp) e C (+11,3pp); (4) score contínuo: BVS pontua consistentemente mais baixo (-15,1 pts ponderado), direção estável mas magnitude variável (-2,6 a -32,7); (5) badrate por rating (só safras maduras): padrão MISTO — BVS MELHOR (menor badrate) que V5 em B e C (~55% da carteira, os 2 ratings com mais volume) nos 4 indicadores, mas PIOR em AA, D1, D2 (inclusive AA, que deveria ser o mais seguro) — não é "BVS ganha em tudo"; (6) KS/Gini (2.3.10, a peça central): BVS SUPERA o V5 nos 4 indicadores SEM EXCEÇÃO (Δ KS +4,5 a +7,6pp, Δ Gini +0,5 a +8,0pp) — o resultado mais limpo/consistente de toda a análise; (7) funil: achei e CORRIGI um erro meu de framing anterior — o Bloco 2.3.9 só calcula o funil REAL (sob V5, que já decide hoje), NUNCA existiu uma simulação "e se fosse o BVS" no notebook; o HTML/PPTX antigos tinham "ASA V5 x ASA BVS" como cabeçalho de tabela do funil sem essa comparação ter sido de fato construída — corrigido pra mostrar só o funil real (contexto de escala: 3M leads/ano, ~60k originações) com nota explícita de que a simulação contrafactual não existe. Recomendação registrada (analítica, não decisão de negócio): "SIM, mas em conjunto" — BVS como camada complementar ao V5, não substituto total — apoiada no KS/Gini (critério mais forte, ganha sempre) e no badrate de B/C (maioria do volume), com 3 ressalvas explícitas (badrate pior em AA/D1/D2, colapso do A2, investigação de vigência ainda aberta). HTML: capítulo 12 inteiro atualizado com números reais, nota geral do topo trocada de "pendente" pra "atualizado com dado real", `<img>` já resolvem pros PNGs reais (paths relativos já estavam corretos desde a correção anterior do checker) — tag balance e todos os 75 `href="#"` revalidados no documento inteiro. PPTX: reescrito do zero via `build_pptx.py` (script, não editado o arquivo binário na mão) — de 12 pra 13 slides (novo slide 9 "Curvas KS — detalhe visual", 2x2 grid das 4 imagens reais, adicionado pra não sobrecarregar o slide 8 de tabelas+imagens); imagens REAIS embutidas via `add_picture()` (antes eram só cards de texto "pendente") nos slides 4 (migração, 2 imgs), 6 (score contínuo, 2 imgs) e 9 (KS, 4 imgs); slide de veredito com checkbox "☑" marcado na opção recomendada; slide do funil corrigido pra não implicar comparação V5xBVS inexistente. Reabri o `.pptx` gerado com `python-pptx` pra confirmar contagem de imagens/tabelas por slide (8 imagens + 5 tabelas no total, batendo com o esperado).
- (02/09/2026) ⚠️ BUG real no 2.3.10, achado pelo responsável rodando: `KeyError: "['contratos_fpd15'] not in index"`. Causa: escrevi as colunas de FPD sem o underscore antes do número (`contratos_fpd15`/`contratos_fpd30`) — o padrão real do projeto (confirmado em `calculo.serie_fpd`/`config.COLUNAS_SUPER_BASE_CONTRATOS`) é `contratos_fpd_{janela}` COM underscore (`contratos_fpd_15`/`contratos_fpd_30`); só o Over é sem underscore (`contratos_over{dias}m{meses}`, ex. `contratos_over30m4`) — inconsistência que não confirmei contra o código já existente antes de escrever o 2.3.10. Corrigido em `_indicadores_ks` (célula `78ca2083`) e no docstring de `calculo.comparar_ks_gini_v5_bvs`. O segundo traceback colado pelo responsável ("Pieces mismatches", `stack_data`) é só um bug interno do IPython tentando formatar a exceção bonitinha — ruído, não relacionado ao bug real. Lição: ao inventar nome de coluna novo baseado em padrão observado em OUTRA família (Over), sempre confirmar contra `grep`/leitura do código já existente da família específica (FPD) antes de assumir o mesmo padrão — não bastava a intuição de "mesma convenção".
- (02/09/2026) Nova seção técnica pedida — "explicar KS, Gini, curva AUC/ROC" pra quem lê com bagagem mais técnica/ML (a seção 12.6 já tinha uma explicação PRA EXECUTIVO, simples de propósito). Adicionada `### 9.7 KS, Gini e curva ROC/AUC — conceitos técnicos` (novo id `ks-gini-tecnico`), no Capítulo 9 "Fórmulas de cálculo" (não no 12, pra não mexer na numeração do capítulo executivo de novo) — cobre: curva ROC/AUC (TPR x FPR, história/origem em radar), KS (teste de Kolmogorov-Smirnov, CDF bons x maus), Gini via curva CAP/Lorenz (não a fórmula 2×AUC−1 direta, mas equivalente), e um `note warn` documentando com precisão a mesma ressalva de sinal já registrada no docstring do `comparar_ks_gini_v5_bvs` (ASA usa "maior=mais seguro", oposto da convenção comum de PD-score "maior=mais arriscado", por isso o `abs()` no Gini). Nav do capítulo 9 atualizado (9.7 adicionado depois de 9.6). Tag balance e todos os `href="#"` (68, 0 quebrados) conferidos de novo no documento inteiro.
- (02/09/2026) Nova visão pedida antes do rerun: "mix" de rating por anomes (peso % de cada rating na originação mensal, ASA V5 x ASA BVS, mesma população `df_originado_ambos_scores`) — diferente da migração (2.3.5, que olha pra ONDE cada CPF foi); esta olha COMPOSIÇÃO/VOLUME por período. Nova `calculo.composicao_rating_por_periodo(df, col_periodo, col_rating)` (crosstab período×rating normalizado por LINHA, exclui vazio/nao_trabalhavel, mesmo critério do QUADRO 5) + nova `### 2.3.11` no notebook (entre 2.3.10 e Bloco 3, ids `3f6ff6eb`/`ef15128b`) — mostra mix V5 e mix BVS lado a lado + tabela de delta (BVS − V5) célula a célula. Depende de `_reordenar_rating_asa` do 2.3.7 (mesma dependência entre células já usada em outros pontos). Pedido explícito do responsável era só pro PPTX ("só queria adicionar mais uma visão no pptx") — NÃO adicionei ao HTML desta vez (evitei a cascata de renumeração 12.3→12.8 sem pedido explícito); ofereci fazer se quiser. PPTX regenerado a partir do script (`build_pptx.py`, não editado o `.pptx` na mão) com um novo slide 5 "Mudou o mix de risco ao trocar de score?" (card metodológico + tabela resumo por rating: peso médio V5/BVS/Δ/maior Δ mensal — não a grade completa mês×rating, que fica densa demais pra slide, mencionada como "detalhe no notebook/HTML"), empurrando os demais slides pra 6-12; slide 2 (agenda) ganhou o item correspondente. Sessão sem ultracode (desligado nesse turno) — trabalho direto, sem workflow, dado o escopo pequeno e já ter os padrões estabelecidos dos turnos anteriores. Responsável confirmou querer o HTML também logo em seguida — adicionada nova `### 12.3 Mix de rating por mês` (id `mix-rating-v5-bvs`), empurrando score-contínuo→12.4, badrate→12.5, ks-gini→12.6, efeito-funil→12.7, veredito→12.8. Renumeração feita com técnica de 2 fases (original→token único `§§N§§`→número final) pra evitar cascata de substituição (ex. trocar "12.4"→"12.5" direto faria a PRÓXIMA troca "12.5"→"12.6" pegar esse resultado por engano) — validado que a contagem de ocorrências bate exatamente igual nas 2 fases (4/4/8/5/5). Tag balance do documento inteiro conferido de novo (tudo 1:1) e todos os 65 `href="#"` resolvendo (37 ids, 0 quebrados). Textos de contagem ("sete seções"→"oito seções" na intro do capítulo, "seis anteriores"→"sete anteriores" no veredito) também corrigidos.
- (01/09/2026) Início da entrega executiva (Etapa 5.6 "Compreensão dos Resultados") — o responsável definiu o objetivo: apresentação comparando ASA V5 x ASA BVS por anomes e rating, pra decidir com executivos se o BVS é "muito ruim ou se podemos utilizar". 3 entregáveis pedidos: (1) gráficos novos no HTML técnico, (2) dashboard PPTX no padrão de `31-07-26 - [AE] Cascata PA BVS/.../Política PA BVS - Resumo Executivo.pptx`, autorização explícita pra criar visões que faltarem e reordenar títulos/subtítulos do notebook. Pedido explícito de workflow ENXUTO (ultracode+xhigh ligados, mas "não criar agentes/tasks desnecessárias"). Progresso desta sessão: **código novo** — `calculo.comparar_ks_gini_v5_bvs()` (reaproveita `ks_gini()`, nunca usado antes; Gini reportado em `abs()` já que o sinal cru depende de convenção "maior=mais arriscado" que ASA não segue — decisão documentada no docstring, não validada por execução real ainda) + `visualizacao.plot_ks_v5_bvs()` (gráfico KS clássico lado a lado V5 x BVS, paleta reaproveitada do 2.3.6: azul `#3C78D8`/verde `#8FDB00`); nova seção **### 2.3.10. KS/Gini** no notebook (Etapa 5.6, inserida entre 2.3.9 e o Bloco 3 — únicos cell IDs afetados: `7c1e011d`/`78ca2083`), roda Over30M4/60M6/FPD15/30 via `df_originado_ambos_scores`; `fig.savefig()` adicionado nas figuras do 2.3.5 (2 heatmaps de migração) e 2.3.6 (2 linhas de score) que antes só faziam `plt.show()` — tudo salvando em `3 - Analytics/outputs/graficos_html/`, pasta nova, pensada pra alimentar HTML/PPTX. Workflow (6 agentes, rodando em background, run_id `wf_7ec7169e-93c`): extrai guia de estilo do template PPTX (paleta/tipografia/padrões de slide) + desenha e revisa (criador→revisor adversarial) um roteiro de 10-14 slides executivos + redige e revisa um novo capítulo 12 do HTML ("Comparação executiva") + checagem final de consistência entre os 3. Instrução dura dada a TODOS os agentes do workflow: praticamente nenhum número real existe ainda (base mudou várias vezes nesta sessão — cache, QUADRO 5 trocado pra ambos_scores, 2.3.10 novo) — proibido inventar número, usar placeholder explícito "pendente_rerun" em tudo que depende de dado. Extraí eu mesmo (fora do workflow, evitando gastar agente numa tarefa mecânica) o guia de estilo visual EXATO do template via `python-pptx` (fonte "Quattrocento Sans" em tudo; paleta: preto quase-puro `#0B0B0B` texto principal, verde `#006300`/`#1BAF7A` kicker/destaque, azul `#2A78D6`, laranja `#EB6834`, cinza `#52514E`/`#898781` corpo/rodapé, divisor `#E1E0D9`, cards brancos com barra de cor à esquerda de 0.06in; slide 13.33x7.5in) — usado como base real pra montar o PPTX novo, independente do que o agente de estilo confirmar. **Pendente**: aguardar o workflow (não fiz polling, só notificação automática), depois montar o `.pptx` de verdade com `python-pptx` usando o roteiro final + inserir o capítulo 12 no HTML (`</section>` antes do `<footer class="doc">`, `<li>` antes de `</ol></nav>`) + o responsável precisa rodar o notebook do zero (kernel limpo, cache já corrigido) pra gerar os PNGs e os números reais que faltam em quase tudo.
- (01/09/2026) ✅ Entrega executiva CONCLUÍDA (estrutura — números pendentes de rerun, ver entrada acima). Workflow `wf_7ec7169e-93c` voltou com roteiro de 12 slides (criador+revisor, 10 correções documentadas pelo revisor — ex. contagem "cinco lentes" que não batia com 8 itens, falta de ponte explicando por que KS/Gini não é redundante com badrate por rating) + capítulo 12 do HTML (criador+revisor) + checagem final, que encontrou 4 problemas REAIS: (1) `<img src="graficos_html/...">` com caminho relativo QUEBRADO — corrigido pra `../3 - Analytics/outputs/graficos_html/...` (capítulo fica em `1 - Documentações/`, imagens em `3 - Analytics/outputs/`, checker comparou contra `config.py` de verdade pra achar isso); (2) seção "Efeito no funil" (slide 9 do roteiro) tinha sumido do HTML — adicionada como nova 12.6 "Efeito no funil", renumerando Veredito de 12.6→12.7 (e todas as referências cruzadas internas); (3) item 5 dos "próximos passos" (plano de piloto pós-aprovação) tinha sumido no HTML — devolvido; (4) o objeto estruturado `paleta` do guia de estilo só tinha 6 cores, faltavam ~10 (accents dos cards, verde do kicker, cinzas de tabela) que só estavam em texto livre — não bloqueou nada porque eu já tinha extraído essas cores direto do `.pptx` original via `python-pptx` antes mesmo do workflow terminar (paralelizado, sem gastar agente numa tarefa mecânica). Depois de corrigido: HTML inserido de verdade no arquivo real (capítulo 12, 7 sub-seções 12.1-12.7, nav lateral atualizado, rodapé "Última atualização" → 01/09/2026) — tag balance validado no doc INTEIRO (div/p/h2/h3/h4/table/tr/th/td/a/b/code/span/section, tudo 1:1) e todos os 61 `href="#..."` internos confirmados resolvendo pra um `id` real (0 links quebrados). PPTX gerado do zero com `python-pptx` (não copiado do template — reconstruído no mesmo estilo visual: fonte Quattrocento Sans, paleta extraída, header kicker+título+divisor, cards com barra de cor lateral, tag laranja "PENDENTE DE RERUN" nos slides com número fictício) — salvo em `1 - Documentações/ASA BVS x ASA V5 - Resumo Executivo.pptx`, 11 slides (capa, agenda, cobertura, migração, score contínuo, badrate Over+FPD — MESCLADOS numa tabela só de 9 colunas, simplificação minha em relação ao roteiro que os mantinha em 2 slides separados —, KS/Gini, funil, alerta de qualidade de dado, veredito, próximos passos); reaberto com `python-pptx` depois de salvo pra confirmar que todas as 11 slides/tabelas vieram certas. TUDO isso é estrutura/placeholder — nenhum número real existe ainda em nenhum dos 2 artefatos, por design (instrução dada a todos os agentes do workflow). Marco zero pro rerun: `[XX% — a confirmar]` aparece em cobertura, migração, score, badrate (56 células), KS/Gini (16 células) e funil; o parágrafo de veredito também é 100% template.
- (01/09/2026) QUADRO 5 (rating ASA, Blocos 2.3.7/2.3.8) trocado de `df_originado_geral` pra `df_originado_ambos_scores` — decisão que já estava documentada como "discutida mas não implementada" na Seção 10.4 do HTML (cada score usava sua própria base de cobertura, tamanhos diferentes — V5 43.816 x BVS 37.331 contratos, ~15%). O responsável confirmou primeiro que hoje QUADRO 5 usa a visão Geral (não Ambos), depois pediu a troca explicitamente, "e deixar sinalizado no código". Mudança: `df_rating_v5`/`df_rating_bvs` (linha que alimenta as 6 tabelas do QUADRO 5, nos 2 blocos) passaram de `calculo.sem_categorias(df_originado_geral, ...)` pra `calculo.sem_categorias(df_originado_ambos_scores, ...)` — comentário no código expandido de 1 linha pra um bloco de 4-5 linhas explicando a troca e datando a decisão (01/09/2026), não só descrevendo o que o código faz. Seção 10.4 do HTML reescrita (era "aviso de tamanhos diferentes, não implementado" → agora descreve a decisão como tomada, com a razão: garantir que qualquer diferença entre as tabelas V5/BVS venha só da CLASSIFICAÇÃO de rating, não de qual contrato entrou em cada base) — nav lateral do HTML atualizado junto (10.4 "Cobertura V5 x BVS no QUADRO 5" → "QUADRO 5 usa Ambos scores"). Validado: tag balance do HTML (div/p/h3/section/table/tr) continua batendo 100% depois da edição; as 2 células do notebook compilam e usam `df_originado_ambos_scores` corretamente. Efeito colateral esperado (não medido ainda): QUADRO 5 vai ficar com volume menor por rating (perde quem só tem 1 dos 2 scores preenchido) — outputs das 2 células precisam ser re-rodados.
- (01/09/2026) ✅ RISCO DE CACHE DESATUALIZADO investigado e resolvido — o responsável tentou validar manualmente um CPF (`05639803037`) que aparecia com `flag_rating_asa_bvs == "vazio"`, consultando direto `df_bvs_funil`/`df_asa_bvs_funil` por CPF sem zero à esquerda (`'5639803037'`) e não achou. 2 achados: (1) `df_asa_bvs_funil['cpf']`/`df_bvs_funil['cpf']` NÃO vêm com padding garantido da Redshift (`extracao.py` faz `SELECT b.*`, e o `LPAD(...)` só existe dentro do `ON` do JOIN, nunca aplicado à coluna de saída) — explica o "não achei" da consulta manual, mas NÃO é bug do pipeline real: `tratamento._preparar_fonte_super_base` já faz `.str.zfill(11)` nas 4 fontes ANTES do merge (`tratamento.py:149`), então `df_super_base_funil`/`flag_match` sempre estiveram corretos. (2) Mais sério: os `.parquet` de cache de `busca_por_cpfs_vigentes` (`df_bvs_funil__9a56a7e91d`/`df_asa_bvs_funil__9a56a7e91d`) estavam datados de 26/08, um dia antes da última alteração salva em `extracao.py` (27/08 20:34) — e a célula 13 do notebook chama a função com `usar_cache=True` (default), sem override, então estava lendo o cache velho sem re-consultar a Redshift; a chave do cache é um hash das LISTAS presentes no funil, não da lógica de extração, então uma mudança no `extracao.py` não invalida o cache sozinha. Resolvido: responsável rodou `extracao.busca_por_cpfs_vigentes(df_funil_tagueado, usar_cache=False)` manualmente — extração fresca achou MAIS matches que o cache velho (bvs: 1.869.863→1.883.412, +13.549; asa_bvs: 1.878.336→1.891.934, +13.598), cache sobrescrito com dado de hoje. `df_super_base_funil` remontado (2.543.758 linhas × 117 colunas) e 3.4 re-rodado com o dado fresco: resultado ainda mais limpo (`flag_match` só tem `"ambos"`/`"nenhum"` na base inteira — `"so_bvs"`/`"so_asa_bvs"` não aparecem NENHUMA vez; `sem_match_bvs == sem_match_asa_bvs` linha a linha por lista), confirmando de novo (com dado atualizado) que "vazio" é sempre falta de match genuína, nunca match-com-score-nulo. Nota lateral: a re-execução em sequência (sem restart de kernel) causou um `MemoryError` transitório no merge da 2.3.2 (fragmentação de memória de DataFrames antigos acumulados, não falta real de RAM — mesmo padrão de `MemoryError` já visto antes nesse projeto) — resolvido com restart do kernel. ⚠️ Ponto de atenção geral pra próximas rodadas: `busca_por_cpfs_vigentes` usa cache por padrão (`usar_cache=True`) e a chave não versiona a lógica de extração — depois de qualquer mudança em `extracao.py` que afete a query/matching, vale rodar 1x com `usar_cache=False` antes de confiar no resultado.
- (31/08/2026) ✅ Validação 3.4 rodada e CONFIRMADA sem bug — o responsável colou o resultado: `flag_rating_asa_bvs == "vazio"` (11.991 linhas) cai 100% em `flag_match == "nenhum"`, zero casos de "vazio" com `flag_match == "ambos"` (que seria o cenário suspeito — match achado, score nulo mesmo assim). Fecha a dúvida levantada olhando o Bloco 2.3.4: `funil_flag_lista_vigente` preenchido + `flag_rating_asa_bvs` vazio É o comportamento esperado (falta de match genuína no JOIN com `asa_bvs_lista_papn`), não uma inconsistência. Achado lateral (não é problema, só característica dos dados): as categorias `"so_bvs"`/`"so_asa_bvs"` de `flag_match` não apareceram NENHUMA vez nesse crosstab — só `"ambos"`/`"nenhum"` — sugere que, nesse recorte, a cobertura de BVS e ASA BVS anda sempre junta (CPF está nas duas tabelas ou em nenhuma, nunca só numa).
- (31/08/2026) 2 ajustes estruturais no notebook, a pedido do responsável: (1) Bloco 3 estava tudo numa única célula de código gigante (3.1-3.4 misturados) — dividido em 4 pares título(`##`)+código, cada um com seu próprio docstring curto (antes era 1 docstring genérico cobrindo os 4). Segue o mesmo padrão dos outros blocos (2.3.6, 2.3.7 etc. — markdown título + célula de código própria). (2) `### 2.1.3.` e `### 2.1.4.` estavam sem nome (só o número) — nomeados "EXPLAIN — estimar linhas da base BVS/ASA BVS (rodada mais recente)", olhando o código de cada célula (`conexao.explain(...)` filtrado pela última data de cada tabela) — esse EXPLAIN é o mesmo que o MEMORY.md já registra como tendo SUBESTIMADO o volume real (~25-30MM linhas, só confirmado depois via GROUP BY direto). Notebook foi de 162 pra 169 células (162 -1 célula gigante +8 novas do Bloco 3).
- (31/08/2026) Adicionado 3.4 ao Bloco 3 — o responsável, olhando o Bloco 2.3.4 (flags de rating), estranhou CPFs com `funil_flag_lista_vigente` preenchido mas `flag_rating_asa_bvs == "vazio"` ("não faz sentido"). Investigação: NÃO é necessariamente bug — `funil_flag_lista_vigente` é atribuído a TODO lead do funil por data (`tratamento.atribuir_lista_vigente`), independente de achar match na ASA BVS; já existe uma coluna `flag_match` (criada em `tratamento.montar_super_base_funil`, valores `"ambos"/"so_bvs"/"so_asa_bvs"/"nenhum"`, baseada em achar ou não a DATA da ASA BVS no JOIN) que já serve pra diferenciar "sem match" de "match achado mas score nulo". 3.4 cruza (`pd.crosstab`) `flag_rating_asa_bvs` (vazio/preenchido) x `flag_match` — se "vazio" cair majoritariamente em `so_bvs`/`nenhum`, é falta de match mesmo (esperado); se aparecer "vazio" com `ambos`/`so_asa_bvs` (achou o registro na ASA BVS mas o score em si veio nulo), aí sim é um caso pra investigar na fonte. Nota adicional no print: ASA V5 é DIFERENTE — `funil_asa_v5` vem direto do funil (não é JOIN separado como a ASA BVS), então "vazio" ali não tem um `flag_match` equivalente pra cruzar, é simplesmente o campo nulo já na origem.
- (31/08/2026) Nova subseção 10.4 no HTML ("QUADRO 5 usa bases de tamanhos diferentes pra V5 e BVS") — o responsável perguntou (sem pedir mudança de código, só entendimento) se faz sentido comparar `df_rating_v5`/`df_rating_bvs` (QUADRO 5) tendo tamanhos diferentes (43.816 vs 37.331 contratos, ~15% de diferença — BVS tem cobertura menor por ser o score novo). Resposta dada e documentada: depende da pergunta — pra checar se os 2 scores ORDENAM o risco parecido (objetivo do QUADRO 5), o formato da curva dentro de cada score importa, não o tamanho da base; o problema apareceria só se alguém comparasse magnitude agregada entre os scores direto dessas tabelas (mesma ressalva do Δ já documentada na 10.2). Também documentada a alternativa pra uma comparação "justa" (restringir a `df_originado_ambos_scores` em vez de `df_originado_geral`, trocando tamanho de base por comparabilidade) — discutida mas NÃO implementada, decisão de manter como está por ora.
- (31/08/2026) Subetapa 5.6 renomeada de "KS/Gini" pra "Compreensão dos Resultados" (pedido direto do responsável, `ATA.html`) — reflete que KS/Gini nunca foi um compromisso firme, só um nome herdado do planejamento inicial ("não pensei em como usar isso", registrado antes nesta sessão). Reformulada como um objetivo mais amplo: consolidar uma leitura/conclusão de tudo que 5.1-5.5 já mostraram (matriz de migração, diferença de score, Over, FPD, conversão) sobre o ASA BVS vs ASA V5 — KS/Gini continua mencionado como UMA opção possível (`calculo.ks_gini()` já existe no boilerplate), não mais como o objetivo central da subetapa. Atualizado em todos os pontos do `ATA.html` que citavam "KS/Gini" (tabela de subetapas, linha da Etapa 5 na tabela principal, discovery, ponto de atenção, resumo Trello).
- (31/08/2026) ATA.html atualizada ("//progresso") — estava desatualizada desde 28/08 (tratava 5.2-5.5 como "código pronto, aguardando validação", e o prazo 28/08 como "hoje"). Atualizado: (1) subetapas 5.2-5.5 marcadas ✅ Concluída (foram executadas/refinadas extensivamente com o responsável ao longo desta sessão — correção metodológica de população V5xBVS, safra imatura, nan%, visual, Bloco 3 de validações, doc reestruturada); só 5.6 (KS/Gini) continua pendente, sem abordagem definida. (2) Card "Ponto de atenção" reescrito — prazo original (28/08) já passou 3 dias (hoje 31/08), destacado em vermelho (era âmbar) — mas com nuance: muito trabalho de Etapa 5 avançou nesse meio tempo, o que ainda falta é decidir KS/Gini e rodar as Etapas 6-8 (dependem de negócio, não só código). (3) Discovery da Etapa 5 expandido com os principais marcos técnicos da sessão (sem duplicar o detalhe completo, que fica no MEMORY.md/HTML técnico). (4) Trello summary e rodapé (data) atualizados. Barra de progresso principal (4/8 etapas, 50%) mantida — Etapa 5 só conta como completa quando TODAS as 6 subetapas fecharem, incluindo 5.6.
- (31/08/2026) ⚠️ ROLLBACK da decisão anterior (as 4 tabelas `bons %`/`maus %` no QUADRO 5) — o responsável colou o output real depois de rodar e confirmou visualmente exatamente a redundância que eu já tinha avisado no HTML: `maus %` saiu IDÊNTICO à `% (qtd)` já existente (mesmos números, célula a célula) e `bons %` só espelha `100 - maus %` — "ficou plotado muita informação... a tabela que tínhamos de over já falava isso". Revertido nos 2 blocos (2.3.7 e 2.3.8): removido o bloco `_bm_v5_pct_base`/`_bm_bvs_pct_base`/`pct_bons_*`/`pct_maus_*` e as 4 entradas correspondentes no `exibir_lado_a_lado` do QUADRO 5, voltando pras 6 tabelas originais (2 `% (qtd)` + 4 bons/maus absolutos). HTML (Seção 10.3) revertido junto (volta a dizer "6 tabelas", parágrafos novos removidos). `calculo.cobertura_scores_asa()`/`fmt_brl()` (Bloco 3) NÃO foram afetados — são coisas diferentes, ficam como estão. Lição: eu já tinha identificado a redundância matemática ANTES de implementar (deixei uma nota .info no HTML avisando) mas implementei mesmo assim por o responsável ter pedido explicitamente — em retrospecto, quando eu mesmo já sei que uma métrica pedida é redundante com algo que já existe, vale mencionar ANTES de implementar (não só documentar depois), pra dar chance de decidir sem precisar rodar e ver.
- (31/08/2026) QUADRO 5 (rating ASA, nos 2 blocos 2.3.7/2.3.8) ganhou 4 tabelas novas — `bons %`/`maus %` por rating, pra cada score (ASA V5, ASA BVS), a pedido do responsável: "% de quantos bons e quantos maus temos em relação ao total (daquele segmento e daquele anomes)". Fica 10 tabelas no total (2 % qtd originais + 4 novas + 4 bons/maus absolutos). Calculado como `100 * bons / (bons + maus)` e `100 * maus / (bons + maus)`, célula a célula — mas com um cuidado importante: pra a coluna `Total` (soma de ratings) e linha `Total Geral` (soma de meses) saírem CORRETAS (agregado ponderado, não média ingênua das %), apliquei `_com_total`/`calculo.com_linha_total_geral` nos bons/maus BRUTOS primeiro, e só DEPOIS dividi — mesma lógica já usada em `linha_total_geral_qtd_val`/`linha_total_geral_badrate_v5_bvs`. Isso significa que essas 4 tabelas novas JÁ nascem com `Total`+`Total Geral` (diferente das 2 tabelas `% (qtd)` originais do QUADRO 5, que continuam sem essa linha). ⚠️ Nota que dei ao responsável (não pedida, mas relevante): `maus %` é matematicamente IDÊNTICO à tabela `% (qtd)` já existente do mesmo score (mesma população, mesma fórmula) — não é uma métrica nova, só reexibida numa tabela separada pra leitura direta ao lado de `bons %`. No FPD, reaproveitei os MESMOS `_nan_rating_v5`/`_nan_rating_bvs` (já calculados pras tabelas `% (qtd)`) pra marcar de amarelo essas 4 tabelas novas também — o denominador é o mesmo (`elegivel_fpd_x` = `bons+maus`), então o padrão de nan é idêntico, não precisou recalcular. No Over, usei `imaturos_mes_total` (mesmo padrão das outras tabelas do QUADRO 5 ali). HTML (Seção 10.3) atualizado.
- (31/08/2026) `calculo.cobertura_scores_asa()` (3.3, Bloco 3) ganhou 3 colunas `_pct` (`so_v5_vazio_pct`, `so_bvs_vazio_pct`, `ambos_preenchidos_pct` — cada uma sobre `total`, `.round(1)`) a pedido do responsável, respondendo a pergunta da tabela diretamente em % em vez de precisar dividir contagens na mão. Mudança só no `calculo.py`. No notebook, só a EXIBIÇÃO precisou de ajuste (a função em si não muda como é chamada): a tabela mistura colunas inteiras (contagem) com as 3 novas em %, então `estilizar_tabela_grupos` (que só centraliza cabeçalho, sem formatar número) não bastava mais — encadeei `.format("{:.1f}%", subset=[...])` só nas 3 colunas `_pct`, deixando as de contagem como inteiro puro. `estilizar_tabela_pct_delta` não serve aqui pq formataria TODAS as colunas não-Δ como %, incluindo as de contagem (errado).
- (31/08/2026) `valor_soma` (3.2, Bloco 3) formatado como BRL — nova função pública `calculo.fmt_brl(v)` ("R$ X.XXX,XX", separador de milhar "." e decimal "," ao estilo BR, "--" pra NaN) — mesma técnica de swap `,`/`.` já usada em `_fmt_pct_pt`, só que pública (com nome sem `_`) porque é chamada direto do notebook, não só internamente pelo `calculo.py`. Aplicada via `.map(calculo.fmt_brl)`, substituindo o `.round(2)` anterior (que deixava o valor cru, sem formatação de moeda).
- (31/08/2026) Bug de exibição no Bloco 3 (3.1/3.2): números de contagem saíam tipo "724.0000" em vez de "724" — as colunas `total`/`elegivel`/`fpd` (3.1) e `total`/`valor_nulo`/`valor_zero` (3.2) vêm de `.groupby().agg(..., "sum"/"count")`, mas como as colunas de origem no super base têm algum `NaN` (mesmo raro), pandas promove a coluna inteira pra `float64` — display cru (sem `.astype(int)`, diferente das tabelas "de verdade" do 2.3.7/2.3.8, que sempre usam `contagem_bons_maus`/etc já com `.astype(int)` embutido) mostra os decimais. Corrigido com `.fillna(0).astype(int)` explícito nas colunas de contagem; `valor_soma` (R$, 3.2) só arredondado (`.round(2)`), continua float de propósito (é dinheiro, não contagem).
- (31/08/2026) O notebook tem uma estrutura de títulos MAIOR que eu não tinha notado: só 2 headers `#` (H1) no arquivo inteiro — `"# Bloco 1 — Setup"` (célula 0) e `"# Bloco 2 — EDA"` (célula 20, cobre TODO o resto do conteúdo real, incluindo os 2.3.x da Etapa 5). O responsável apontou que meu "Bloco 3" (criado como `###`, H3) ficava visualmente ANINHADO dentro de "Bloco 2 — EDA" no outline — pediu pra virar independente, `# Bloco 3 — Validações` (H1, mesmo nível de Bloco 1/2, mesmo padrão curto de nome). 2 correções: (1) trocado `###`→`#` e o título pro formato exato "# Bloco N — Nome" dos outros 2; (2) MOVIDO de posição — estava entre o 2.3.8 (FPD) e o 2.3.9 (funil), o que faria o 2.3.9 parecer aninhado DENTRO do novo "Bloco 3" (por vir depois de um header H1); reposicionado pra DEPOIS do 2.3.9 (que é o fim do conteúdo real de "Bloco 2"), antes da leva de ~100 células vazias no final do notebook. Notebook tem 162 células (contagem preservada, só reordenado via `splice`, nada perdido).
- (31/08/2026) Renomeado de "Bloco 2.3.10" pra "Bloco 3" (subitens 3.1 fpd/3.2 over/3.3 cobertura) a pedido do responsável — não é uma continuação da numeração 2.3.x (Etapa 5, blocos 2.3.4-2.3.9), é um bloco de validação separado, numeração própria. Título markdown e os 3 banners internos (`imprimir_banner`) ajustados pra "3.1 fpd — ...", "3.2 over — ...", "3.3 cobertura — ...".
- (31/08/2026) Novo bloco "Validações ad-hoc" (célula markdown `358187bf` + código `6478cbe8`, inserida entre 2.3.8/FPD e 2.3.9/funil, aproveitando uma célula vazia `435886ff` que já existia ali como espaçador) — a pedido do responsável, que listou 3 perguntas concretas depois de eu já ter explicado as causas em prosa/tabela no HTML: (1) causa do nan% no FPD — mostra `elegivel_fpd_x`/`fpd_x`/`pct_elegivel` crus por mês, pra ver exatamente onde a elegibilidade zera; (2) causa do nan% no Over — mostra `valor_originado_bruto` nulo/zero/soma por mês, pra confirmar (ou não) a hipótese de atraso de ETL; (3) por que `df_originado_ambos_scores` é menor que `df_originado_geral` — o responsável reparou que "Ambos scores" tem menos linhas que "Geral" mas não sabia se faltava mais ASA V5 ou ASA BVS preenchido. Nova função `calculo.cobertura_scores_asa(df, col_periodo)` — conta por período quantos têm `v5_vazio`/`bvs_vazio`/`ambos_vazios`/`so_v5_vazio`/`so_bvs_vazio`/`ambos_preenchidos`, respondendo a pergunta 3 diretamente. Célula SEM estilo/destaque de propósito (`display()` cru, não `estilizar_tabela_pct_delta`/`exibir_lado_a_lado`) — é validação/diagnóstico, não um quadro do relatório final; usei `visualizacao.imprimir_banner()` só pra rotular as 3 seções (VALIDAÇÃO 1/2/3, numeração própria, não conflita com QUADRO/GRÁFICO do 2.3.7/2.3.8). Depende de `df_originado_geral`/`df_originado_ambos_scores` (2.3.7 tem que rodar antes, mesmo padrão de dependência entre células já usado no projeto).
- (31/08/2026) Investigando "ainda tem nan aparecendo no 2.7 e 2.8" — comparei os outputs atuais com o mapeamento de 4 causas já documentado: no 2.3.7 (Over), só sobrou o padrão já sinalizado como NÃO resolvido (R$ do mês mais recente, provável atraso de ETL) — `anterior_1a_lista`/`nao_trabalhavel` sumiram de verdade. No 2.3.8 (FPD), o nan que aparece é o padrão "safra imatura" já documentado (`elegivel_fpd_x=0`, comportamento CORRETO) — só que eu não tinha percebido/documentado que ele aparece MUITO mais no QUADRO 5 (rating) que no 2/4, porque quebrar por rating deixa cada fatia menor, e "zero elegível" bate mais cedo em cada rating individual do que no agregado. Usei `AskUserQuestion` pra confirmar o que fazer — resposta: adicionar destaque visual no FPD também, cor diferente (amarelo) do vermelho do Over, com legenda explicando "o que a gente pressupõe que é". Implementado: nova função `calculo.periodos_com_nan(tabela)` (genérica — acha linhas com pelo menos 1 NaN numa tabela JÁ CALCULADA, sem precisar de elegibilidade à parte, já que o FPD embute isso na própria fórmula/denominador) e `visualizacao.marcar_periodos_imaturos()` generalizada com parâmetro `cor` (default `COR_IMATURO` vermelho, mantém comportamento do Over inalterado) — nova constante `COR_NAN = "#eab308"` (amarelo) pro FPD. Aplicado nas 3 tabelas em % do 2.3.8 que podem ter nan (QUADRO 2 qtd x R$ x Δ, QUADRO 4 asa_v5 x asa_bvs, QUADRO 5 rating % qtd) — NÃO nas tabelas de contagem (sempre inteiras, nunca NaN). Legenda "nan% — provável falta de elegibilidade (safra ainda maturando)" condicionada a ter pelo menos 1 nan naquele quadro específico. HTML (Seção 11.2 e a tabela de funções do 11.1) atualizado pra refletir que o FPD agora TEM marcação própria (cor diferente, não "não precisa" como dizia antes).
- (31/08/2026) A reestruturação anterior (capítulos 9/10/11 com subitens 9.1-11.2) só numerava os `<h3>` no CONTEÚDO — o menu lateral continuava plano (só 1-11, sem sub-itens visíveis), o que o responsável apontou como "não temos os 11.1 11.2 etc" (mandou print confirmando: menu com "9 Fórmulas de cálculo"/"10 Comparando V5 x BVS"/"11 Avisos..." mas nenhuma quebra por baixo). Corrigido: os 11 `<h3>` dos capítulos 9-11 ganharam `id` (`fpd`, `over`, `score-continuo`, `faixas-rating`, `migracao-rating`, `conversao-funil`, `pop-geral-ambos`, `pop-v5-bvs`, `quadro-formula`, `safra-imatura`, `nan-explicacao`), e o menu lateral ganhou um `<ol class="sub">` aninhado dentro do `<li>` de cada um desses 3 capítulos, com link pra cada subseção. CSS novo (`nav ol.sub`) com indent maior e fonte menor pros sub-itens; escondido em telas pequenas (`nav ol.sub{display:none}` no media query &lt;900px, onde o menu já vira pills horizontais — sub-itens não cabem nesse layout). O JS de "aba ativa ao rolar" (`querySelectorAll("nav a")`) já pega os novos links automaticamente, sem precisar mexer nele. Validado que os 22 `href` do menu (11 capítulos + 11 subitens) resolvem pra um `id` existente no documento.
- (31/08/2026) Reestruturado o HTML de documentação a pedido do responsável ("dar uma estruturada e organizada melhor... capítulos e subitens melhores"). A antiga Seção 09 "Fórmulas de cálculo" tinha crescido organicamente (11 `<h3>` misturando fórmulas puras, metodologia de comparação V5xBVS e troubleshooting, tudo dentro de 1 `<section>` só, ~400 linhas). Dividida em 3 capítulos: **09 Fórmulas de cálculo** (FPD, Over, Score contínuo, Faixas de rating, Migração de rating, Conversão do funil — só definição+exemplo, 9.1-9.6), **10 Comparando ASA V5 x ASA BVS por população** (df_originado_geral x ambos_scores, asa_v5 x asa_bvs, qual quadro usa qual fórmula — a metodologia de "quem entra na conta", 10.1-10.3), **11 Avisos e particularidades dos dados** (safra ainda maturando, nan% — troubleshooting, 11.1-11.2). Cada `<h3>` ganhou um número tipo "9.1"/"10.2" (`<span class="subnum">`, nova regra CSS baseada no `.num` do `<h2>` já existente). Menu lateral (`<nav>`) ganhou 2 `<li>` novos (Capítulo 10/11) — a lógica de "aba ativa ao rolar" (`IntersectionObserver`) já lê os links do nav dinamicamente por `href`/`id`, não precisou mexer no JS. Adicionado um aviso cruzado (nota `.warn`) no fim do 9.2 (Over) apontando pro 11.1, já que o caveat de safra imatura saiu de perto da fórmula — só o link ficou lá, o conteúdo completo mudou pra 11.1. Conteúdo em si não mudou (só reorganizado/renumerado) — validei via `grep` que nenhum trecho ficou duplicado ou sumiu, e que todas as tags (`div`/`table`/`h3`/`p`/`section`) continuam balanceadas depois da edição grande.
- (31/08/2026) Investigado (a pedido do responsável, "leia os blocos 2.7 e 2.8, tem alguns nan%, por que isso acontece?") e documentado no HTML os 4 padrões de `nan%` encontrados lendo os outputs reais: (1) safra imatura no FPD (`elegivel_fpd_x=0`, comportamento correto/nativo do FPD); (2) `anterior_1a_lista` sem cobertura de ASA BVS (contratos de antes da 1ª lista rastreada, época em que o BVS não existia); (3) rating `nao_trabalhavel` sem nenhum contrato originado (score baixo demais, provavelmente reprovado antes de originar); (4) Over, R$ do mês mais recente com `sum(valor_originado_bruto)=0` — não resolvido, sinalizado como possível atraso de ETL a conferir na fonte. Nova função `calculo.sem_categorias(df, coluna, categorias)` (genérica — remove linhas onde `coluna` está na lista `categorias`) usada pra RESOLVER as causas (2) e (3), a pedido explícito do responsável: `df_originado_geral_lista`/`df_originado_ambos_scores_lista` (novos, definidos 1x antes do loop em 2.3.7, reaproveitados no 2.3.8 como os outros globais) tiram `anterior_1a_lista` e são usados SÓ nas visões por lista vigente (GRÁFICO 3, QUADRO 4 badrate-lista e contagem-lista) — as visões por mês continuam com a base completa. `df_rating_v5`/`df_rating_bvs` (QUADRO 5, nos 2 blocos) passaram a excluir `"nao_trabalhavel"` além de `"vazio"`. O padrão (4) NÃO foi mexido — é sinal de dado, não de código, só documentado.
- (31/08/2026) Depois de tirar o grid, o responsável notou (print) que ainda sobravam linhas verticais finas DENTRO da faixa vermelha de safra imatura — não era mais grid, e sim a emenda entre `axvspan`s adjacentes (1 por mês imaturo): mesmo com a mesma cor/opacidade, a borda antialiased de cada retângulo cria uma linha visível onde dois retângulos vizinhos se tocam. Corrigido agrupando meses imaturos CONSECUTIVOS num único `axvspan` por trecho contínuo (`itertools.groupby` com o truque `índice - posição` pra achar sequências), em vez de 1 retângulo por mês — elimina a emenda porque vira uma faixa só, sem bordas internas. Trechos não-contínuos (ex. imaturo, maturo, imaturo de novo) continuam virando retângulos separados corretamente.
- (31/08/2026) Ajustes nos gráficos de linha (`plot_serie_temporal`), a pedido do responsável olhando print: (1) removidas as linhas de grade — o `ax.grid(axis="y", ...)` só configurava o grid horizontal, mas as linhas VERTICAIS que apareciam vinham do tema global `sns.set_theme(style="whitegrid")` (`bootstrap.py:12`, roda 1x na sessão) — trocado por `ax.grid(False)`, que sobrescreve o estilo global pra esse eixo específico, removendo grade nos dois sentidos. (2) Sombra de safra imatura (`axvspan`) já usa a mesma constante `COR_IMATURO` das tabelas — só precisava rodar de novo pra pegar a troca pra vermelho claro (feita no passo anterior); aumentei a opacidade de 0.15 pra 0.2 pra ficar mais parecida com o destaque das tabelas (que é mais forte, ~30%), sem exagerar numa área tão grande do gráfico (banda vertical inteira, diferente de uma célula pequena de tabela).
- (31/08/2026) ⚠️ BUG real encontrado e corrigido: o destaque de safra imatura (fundo vermelho/âmbar em `marcar_periodos_imaturos`) parou de aparecer DEPOIS que adicionei o fundo branco fixo (`_centralizar_cabecalho`) — o responsável reportou "não foi pintado" com print confirmando (só o itálico aparecia, sem cor nenhuma). Causa raiz: `pandas.Styler.apply()`/`.applymap()` NÃO geram `style=""` inline de verdade — viram regras CSS `#T_xxx_rowN_colM { ... }` (seletor só com ID, especificidade 1-0-0) num `<style>` block. O seletor que eu tinha usado pro fundo branco, `"th, td"`, virava `#T_xxx th`/`#T_xxx td` (ID + TAG, especificidade 1-0-1) — MAIOR que a do destaque por célula — então o branco vencia mesmo aparecendo ANTES no CSS (especificidade sempre decide antes de ordem; ordem só desempata quando a especificidade é IGUAL). Fix: trocar o seletor pra `"*"` (universal — contribui ZERO pra especificidade), que fica com especificidade 1-0-0 igual à do destaque por célula; como aí empata, quem vem DEPOIS no CSS vence, e os destaques por célula são sempre adicionados depois na cadeia de chamadas (`marcar_periodos_imaturos`/`_marcar_total_geral`/`_cor_delta` chamados depois de `estilizar_tabela_*`) — então voltam a aparecer por cima corretamente. Esse MESMO bug provavelmente também estava apagando a cor vermelho/verde do `Δ` (`_cor_delta`, via `.applymap()`) desde que o fundo branco foi introduzido — corrigido junto, sem pedido explícito, já que é a mesma causa raiz. Aproveitei o pedido pra trocar `COR_IMATURO` de âmbar (`#f59e0b`) pra vermelho claro (`#ef4444`), como pedido.
- (31/08/2026) Mais 2 ajustes de cor nas tabelas, a pedido do responsável: (1) a linha de título das colunas (`th.col_heading`/`th.blank` — depois de `_mesclar_nome_indice`, é onde o nome do índice tipo "dt_criacao" mora agora) ganhou fundo próprio (`COR_CABECALHO = "#e2e8f0"`, cinza-azulado claro), diferente do branco das linhas de dado — cuidado tomado pra NÃO pintar a coluna de rótulo de linha (`th.row_heading`, ex. "2025-11"/"Total Geral"), que também é `<th>` mas não é "a linha de título" pedida; regra `th.col_heading, th.blank` é mais específica que o `th, td` genérico, então vence sem depender de ordem. (2) Aumentada a opacidade do destaque de safra imatura (`marcar_periodos_imaturos`) de `{COR_IMATURO}30` (~19%) pra `{COR_IMATURO}4D` (~30%) — o tom anterior ficava quase imperceptível nos prints que o responsável mandou (só dava pra notar pelo itálico, não pela cor). Mesma cor (âmbar), só mais visível — não pediu uma cor diferente explicitamente, só que desse pra enxergar.
- (31/08/2026) 2 ajustes finos nas tabelas do QUADRO 4/9 (asa_v5 x asa_bvs), a pedido do responsável olhando print: (1) título do card ("FPD30 — asa_v5 x asa_bvs por mês (dt_criacao)") era grande demais e "escapava" da largura do card — encurtado pra só "asa_v5 x asa_bvs (dt_criacao)"/"asa_v5 x asa_bvs (dt_vigencia_lista)", tirando `{rotulo} —` e "por mês"/"por lista vigente" (redundante — o banner do QUADRO já diz "FPD30 —" e "por lista vigente"; só esses 2 títulos tinham esse padrão longo, os outros quadros já eram curtos). (2) O nome do índice (`dt_criacao`, de `rename_axis`) aparecia numa 2ª linha de cabeçalho própria, separada da linha com `asa_v5`/`asa_bvs`/`Δ` — pandas sempre renderiza assim (nome do índice numa linha, nomes de coluna noutra), o responsável achou confuso já que são todos "títulos". Novo helper `visualizacao._mesclar_nome_indice(html)` — regex que junta as 2 `<tr>` do `<thead>` numa só, manipulando o HTML já gerado por `Styler.to_html()` (pandas não tem opção nativa pra isso), chamado automaticamente dentro de `exibir_lado_a_lado()` pra TODA tabela que passa por ali (não só essa) — validei o regex contra o HTML real capturado dos outputs antes de aplicar.
- (31/08/2026) Fundo das tabelas forçado pra BRANCO (`th`/`td` com `background-color:#ffffff;color:#1e293b` em `_centralizar_cabecalho()`, chamada por `estilizar_tabela_grupos`/`estilizar_tabela_pct_delta`) — o responsável usa VSCode com tema escuro, e sem fundo explícito a tabela herdava o preto/quase-preto do tema, ficando difícil de ler perto do resto da UI escura. Fundo fixo (não "respeita o tema") é intencional aqui — os destaques de célula (`Δ` colorido, safra imatura em âmbar, borda do Total Geral) usam `style` inline por `.apply()`/`.applymap()`, que tem prioridade sobre esse CSS de tabela, então continuam aparecendo por cima normalmente (inclusive o âmbar de imaturidade fica mais parecido com o mockup que o responsável mandou antes — um tom pêssego claro sobre fundo branco, não mais um verniz sobre preto). Mudança só em `visualizacao.py`, cobre toda tabela estilizada via essas 2 funções (2.3.7 e 2.3.8) automaticamente com `%autoreload 2`.
- (31/08/2026) "Aplique essa visão de tabela bonita nas demais tabelas de inadimplência" — usei `AskUserQuestion` pra descobrir o que exatamente faltava (a barra de título colorida + legenda já cobre TODAS as tabelas do 2.3.7/2.3.8 via autoreload, então não havia "demais tabelas" sem ela); resposta: destacar a linha/coluna `Total Geral` — ela só ficava com cor quando também imatura (fundo âmbar de `marcar_periodos_imaturos`), sem destaque próprio quando madura. Implementado como `visualizacao._marcar_total_geral()` (negrito + `border-top`/`border-left` na cor `COR_TOTAL`, de propósito SEM `background-color` — assim não compete com o fundo âmbar de imaturidade na mesma célula, as duas marcações convivem) — chamado AUTOMATICAMENTE de dentro de `estilizar_tabela_grupos()` e `estilizar_tabela_pct_delta()`, então cobre toda tabela que passa por elas sem precisar editar nenhuma célula do notebook (mudança 100% em `visualizacao.py`, `%autoreload 2` propaga sozinho pra 2.3.7 E 2.3.8).
- (31/08/2026) Ajuste visual em `visualizacao.exibir_lado_a_lado()`, a pedido do responsável (mandou print) — o título de cada bloco (ex. "Geral — contagem") era só texto em negrito sem fundo próprio, então em tema escuro do notebook lia como "uma barra preta" (sem cor nenhuma por trás, só herdando o fundo escuro da célula). Trocado `border-top` fino pela cor de `_CORES_LADO_A_LADO` por uma BARRA cheia (`background:{cor};color:#fff`, cantos arredondados, sombra leve) — a mesma paleta de antes (azul "Geral", verde "Ambos scores", cinza neutro pro resto), só que preenchida de verdade em vez de só uma linha fina. Novo parâmetro `legenda=[(cor, texto), ...]` — desenha quadradinhos coloridos + texto embaixo do conjunto de tabelas; usado nos QUADRO 2/4/5 do 2.3.7 pra explicar o destaque de `marcar_periodos_imaturos` ("sem maturação"), condicionado a ter pelo menos 1 período imaturo naquele quadro (`legenda=(...) if imaturos_mes_total else None`). Como é mudança só no `visualizacao.py`, TODAS as chamadas de `exibir_lado_a_lado` do notebook (2.3.7 E 2.3.8, Over e FPD) ganham a barra colorida automaticamente com `%autoreload 2`, não só os quadros do Over.
- (31/08/2026) Implementada eligibilidade pro Over (não existia antes, diferente do FPD) — o responsável percebeu que "Geral — contagem" do QUADRO 2 tinha `maus=0` a partir de 2026-04 e perguntou por quê. Investigação (agent de pesquisa no código) confirmou: Over30M4 precisa de 4 meses de observação desde a originação pra um contrato ter tido CHANCE de dar Over; contratos de abril/2026 em diante (hoje = 31/08/2026) ainda não completaram essa janela — é censura à direita, não risco real. Diferente do FPD (que já vem com `contratos_elegivel_fpd_x` pronto do Redshift), o Over não tem esse conceito upstream — `calculo.serie_over()` sempre divide pela base TOTAL, então safra imatura aparece com badrate artificialmente baixo/zero. Solução (pedida explicitamente como "marcar como não-maturado", NÃO mudar a fórmula): 2 funções novas em `calculo.py` — `elegibilidade_over(df, janela)` (adiciona `contratos_elegivel_over{dias}m{meses}` por contrato, comparando `funil_data_criacao + meses <= hoje`) e `periodos_imaturos(df, col_periodo, janela)` (conjunto de períodos com cobertura de elegibilidade < 100%); 2 helpers novos em `visualizacao.py` — `marcar_periodos_imaturos(styler, periodos)` (pinta linhas de âmbar+itálico nas tabelas, encadeável depois de `estilizar_tabela_*`) e `plot_serie_temporal(..., periodos_imaturos=...)` (sombreia colunas do eixo X + legenda "safra ainda maturando"). Aplicado em TODAS as tabelas/gráficos do Bloco 2.3.7 que mostram Over por período (GRÁFICO 1, QUADRO 2, GRÁFICO 3, QUADRO 4, QUADRO 5) — não só onde a dúvida surgiu, pra manter consistência (mesmo raciocínio de "aplicar em todas" de pedidos anteriores). A linha/coluna `Total Geral` também é marcada quando há algum período imaturo contribuindo pro agregado. Print de aviso adicional (`⚠️ {rotulo}: safra ainda maturando...`) logo no início de cada iteração do loop, listando os meses afetados. NÃO aplicado no 2.3.8 (FPD) — lá a elegibilidade já é nativa (`contratos_elegivel_fpd_x`) e já é usada como denominador, não tem o mesmo problema. Nova Seção no HTML ("Safra ainda maturando") com a explicação completa + tabela das 4 funções/helpers, inserida logo após a explicação da fórmula do Over.
- (31/08/2026) Removido o gráfico da visão de rating ASA (GRÁFICO 5, barras empilhadas) nos 2 blocos (2.3.7/2.3.8), a pedido do responsável — ficaram só as tabelas (QUADRO, que virou # 5 automaticamente já que a numeração é por `contador_bloco` incremental, não hardcoded). `plot_barras_empilhadas` REMOVIDA do `visualizacao.py` (ficou sem nenhum uso depois disso — não deixei código morto). Numeração final: Over30M4/FPD15 = banners 1-5, Over60M6/FPD30 = 6-10 (era 1-6/7-12). `tabela_rating_v5`/`tabela_rating_bvs` continuam sendo calculadas (usadas nas colunas % (qtd) do QUADRO 5), só o `plt.subplots`+`plot_barras_empilhadas`+`plt.show()` foi removido. HTML (Seção 09) e docstrings das 2 células atualizados.
- (31/08/2026) Correção do pedido anterior: o responsável não queria barras AGRUPADAS, e sim EMPILHADAS ("Falei em colunas 100% empilhadas e não agrupadas"). Usei `AskUserQuestion` pra confirmar o que exatamente empilhar (a métrica Over%/FPD% por rating não soma 100% entre ratings, então "100% empilhada" literal não fazia sentido matemático) — resposta: "1 barra por mês, dentro dela cada cor por rating, e a % de over ou fpd" — ou seja, empilhar o VALOR real de cada rating (não normalizado a somar 100%; o topo da pilha não tem significado próprio, é só uma forma compacta de comparar ratings dentro do mesmo mês). Renomeada `plot_barras_agrupadas` → `plot_barras_empilhadas` (mesmo arquivo/assinatura, só troca `ax.bar(x+deslocamento, ...)` por `ax.bar(x, valores, bottom=base, ...)` acumulando `base` a cada rating). Docstring deixa explícito que NÃO é normalizado — pra não confundir quem ler o código achando que é um % stacked chart de verdade.
- (31/08/2026) 2 pedidos do responsável sobre a visão de rating ASA: (1) trocar o gráfico de LINHA por barras no GRÁFICO 5 — "não fica legal de ver" com 7-8 ratings cruzando o gráfico inteiro (ver correção de "agrupada"→"empilhada" acima). (2) Replicar a visão de rating (GRÁFICO 5/QUADRO 6) pro Bloco 2.3.8 (FPD) também — até agora só existia no 2.3.7 (Over). Bloco novo no 2.3.8 é cópia adaptada (tipo="fpd", `calculo.serie_fpd` em vez de `serie_over`), REAPROVEITANDO os helpers `_reordenar_rating_asa`/`_com_total`/`_com_total_geral` já definidos no 2.3.7 (não redefinidos no 2.3.8 — mesma lógica de dependência entre células que já existia pra `df_originado_geral`/`df_originado_ambos_scores`: precisa rodar 2.3.7 antes). Numeração dos 2 blocos agora é simétrica: Over30M4/FPD15 = banners 1-6, Over60M6/FPD30 = 7-12 (2.3.8 ganhou os blocos 5-6 que faltavam). HTML (Seção 09) atualizado removendo a ressalva "só no 2.3.7 por ora".
- (31/08/2026) `calculo.contagem_bons_maus()` (usada nas tabelas "Geral/Ambos scores — contagem" do QUADRO 2 e QUADRO 4) ganhou uma coluna `"Total Geral"` (= `bons + maus`, idêntica à coluna `total` já existente) — o responsável pediu "a mesma lógica" do QUADRO 6, que tem `Total`/`Total Geral` como coluna (lá bons/maus são tabelas SEPARADAS por rating, então `Total Geral` consolida as duas; aqui já vêm juntas em `total`, então é redundante em valor mas mantém o nome consistente entre os quadros). Mudança só no `calculo.py` — como o notebook usa `%autoreload 2`, as 8 chamadas de `contagem_bons_maus()` (QUADRO 2/4, Over e FPD) pegam a coluna nova sem precisar editar o `.ipynb`; só limpei os outputs das 2 células pra não ficar com print desatualizado. Confirmado com o responsável via `AskUserQuestion` antes de implementar (a mensagem inicial dele — "tem total geral na coluna e não na linha" sobre uma tabela que JÁ tinha a linha — estava ambígua; a pergunta esclareceu que era pedido de CONSISTÊNCIA com o Quadro 6, não uma correção de bug).
- (31/08/2026) Depois de eu confirmar que `Total Geral` já estava certo nas tabelas de contagem (Q2/Q4), o responsável apontou (colando um print/texto) que faltava nas tabelas EM % — `qtd x R$ x Δ` (QUADRO 2) e `asa_v5 x asa_bvs` (QUADRO 4). Diferente da contagem (soma direta), % não pode ser somada nem ter média simples das % mensais — implementei como "recalcular a fórmula com os agregados de TODOS os meses somados primeiro" (ponderado pelo volume real, não uma média ingênua que trataria um mês de 50 contratos igual a um de 5000). 2 funções novas: `calculo.linha_total_geral_qtd_val(df, tipo, janela)` (Over/FPD agregado — vira a linha do QUADRO 2) e `calculo.linha_total_geral_badrate_v5_bvs(df_originado, tipo, janela)` (asa_v5/asa_bvs/Δ agregados, mesma regra de população separada por score da `tabela_badrate_v5_bvs_por_periodo` — vira a linha do QUADRO 4, e é a MESMA linha nas 2 tabelas "por mês"/"por lista vigente", já que agregar tudo ignora a coluna de período usada). Aplicado nos 2 blocos (Over e FPD). NÃO aplicado na % por rating (QUADRO 6) — juntar os ratings de novo anularia o propósito da quebra por rating ali.
- (31/08/2026) `calculo.com_linha_total_geral()` (linha, criada no passo anterior pro QUADRO 2/4) aplicada TAMBÉM nas 4 tabelas de bons/maus do QUADRO 6 (rating ASA) — o responsável pediu "adicionar em todas" depois de checar o output de Q2/Q4 (confirmei via HTML gerado que a linha JÁ estava lá — provavelmente só passou despercebida por ser a última linha). No QUADRO 6, cada tabela de bons/maus agora tem tanto a COLUNA `Total Geral` (bons+maus da MESMA linha/mês, de `_com_total_geral`, decisão anterior) quanto a LINHA `Total Geral` (soma de todos os meses, de `calculo.com_linha_total_geral`) — o canto inferior-direito vira "total geral dos totais gerais", que é o mesmo número em todas as 4 tabelas (bons+maus, todos os ratings, todos os meses) — comportamento esperado de tabela com total de linha E coluna. NÃO aplicado nas tabelas em % (qtd x R$ x Δ do QUADRO 2, asa_v5 x asa_bvs do QUADRO 4, % (qtd) por rating do QUADRO 6) — somar percentual ao longo dos meses não tem significado (não é uma métrica acumulável), só faz sentido pras tabelas de contagem absoluta (total/bons/maus).
- (31/08/2026) Tabelas de contagem (`calculo.contagem_bons_maus` — total/bons/maus SEM quebra por rating, usadas nos QUADRO 2 e QUADRO 4 dos Blocos 2.3.7 E 2.3.8, "Geral"/"Ambos scores") ganharam uma LINHA `"Total Geral"` no final (soma de cada coluna ao longo de todos os períodos), a pedido do responsável olhando a tabela "Geral/Ambos scores — contagem" do QUADRO 4. Diferente do `Total Geral` do QUADRO 6 (que é uma COLUNA, soma bons+maus da MESMA linha/mês, porque lá bons e maus são tabelas separadas) — aqui já existe uma coluna "total" = bons+maus por linha; o que faltava era o agregado de TODOS os meses/listas juntos, por isso virou uma linha, não coluna. Nova função `calculo.com_linha_total_geral(tabela)` (genérica — soma todas as colunas, funciona pra qualquer tabela numérica com índice = período), aplicada nas 8 chamadas de `contagem_bons_maus` do QUADRO 2/4 nos 2 blocos (Over e FPD) — mantendo os 2 blocos estruturalmente iguais, mesmo o pedido citando só a tela do Over.
- (31/08/2026) Tabelas de bons/maus do QUADRO 6 (rating ASA, 2.3.7) ganharam mais uma coluna, `Total Geral`, à direita da `Total` já existente — `Total` = soma dos ratings NAQUELA tabela (só bons OU só maus); `Total Geral` = `Total(bons) + Total(maus)` daquele mês, ou seja, o total de contratos (bons+maus) da população, igual nas 2 tabelas (bons e maus mostram o mesmo valor de `Total Geral`, serve de referência cruzada/conferência). Novo helper `_com_total_geral(bm)` (recebe o dict `{"bons":..., "maus":...}`, soma as colunas `Total` das duas e adiciona `Total Geral` em ambas via `.assign`) — chamado depois de `_com_total`/`_reordenar_rating_asa` já terem rodado.
- (31/08/2026) GRÁFICO 5/QUADRO 6 (rating ASA, 2.3.7): 2 ajustes a pedido do responsável. (1) Ordem das colunas de rating estava alfabética default do pandas (unstack) — trocada pra ordem de risco melhor→pior: `AA, A1, A2, B, C, D, E`, usando `list(reversed(config.FAIXAS_RATING_ASA))` (a tabela oficial já está na ordem pior→melhor, `E` primeiro) — novo helper `_reordenar_rating_asa()` (definido 1x antes do loop de janela) reindexed as colunas nessa ordem (mantendo qualquer categoria fora da lista, tipo "nao_trabalhavel"/">1000", no final, sem perder dado) e renomeia `D`→`D1`, `E`→`D2` (pedido do responsável, nomes mais claros que letra solta). Aplicado no gráfico (2 linhas) E nas 6 tabelas do QUADRO 6 — mesma ordem em tudo. (2) As 4 tabelas de bons/maus (absolutas) ganharam coluna `Total` (`tabela.sum(axis=1)`, helper `_com_total()`) — soma de todos os ratings daquele mês, pra dar o número agregado sem precisar somar manualmente. Ambos helpers só existem no 2.3.7 por enquanto (mesma situação da visão em si — não replicado pro 2.3.8/FPD ainda).
- (31/08/2026) QUADRO 6 (rating ASA, 2.3.7) recebeu mais 2 tabelas: o responsável notou que só tinha bons/maus, faltava o % (qtd) de Over por rating em tabela (só tinha no gráfico). Adicionadas `tabela_rating_v5`/`tabela_rating_bvs` (as mesmas usadas no gráfico do QUADRO/GRÁFICO 5, já calculadas ali — só reaproveitadas) como as 2 primeiras tabelas do QUADRO 6 (antes das 4 de bons/maus, seguindo a convenção "taxa/% antes de absoluto" dos outros quadros) — total 6 tabelas lado a lado. `estilizar_tabela_pct_delta` funciona sem coluna "Δ" também (só formata tudo como % — não tem Δ aqui pq não é uma comparação V5 x BVS numa mesma tabela, são 2 tabelas separadas por rating).
- (31/08/2026) Nova visão no Bloco 2.3.7 (Over), a pedido do responsável: Over por rating ASA (V5 e BVS) por mês — retoma a ideia original de comparar se os 2 scores ORDENAM o risco parecido (que tinha virado o heatmap, depois removido). Desta vez como 2 gráficos de linha lado a lado (`plt.subplots(1,2)`, 1 linha por rating dentro de cada) em vez do heatmap, reaproveitando `calculo.serie_over(colunas_grupo="flag_rating_asa_v5"/"flag_rating_asa_bvs")` — já suportava quebra por coluna categórica, não precisou de função nova pro gráfico. Cada score filtrado pra sua PRÓPRIA população (`flag_rating_asa_v5/bvs != "vazio"`, sem exigir o outro) — mesma regra de população isolada por score já usada em `tabela_badrate_v5_bvs_por_periodo`. Nova função `calculo.contagem_bons_maus_por_grupo()` (pivota bons/maus por período x grupo, ex. mês x rating) pra dar o quadro de bons/maus por rating logo abaixo do gráfico (4 tabelas lado a lado: V5-bons, V5-maus, BVS-bons, BVS-maus) — evita ler diferença de Over por rating que na verdade é N pequeno numa faixa. Vira GRÁFICO 5/QUADRO 6 dentro do loop de janela (Over30M4 = banners 1-6, Over60M6 = 7-12 — renumeração automática via `contador_bloco`). ⚠️ Implementado SÓ no 2.3.7 por enquanto — o pedido do responsável mencionou especificamente "over"; ainda não replicado pro 2.3.8 (FPD), avisar se quiser espelhar lá (mesmo padrão de outras vezes: "vamos usar isso pro FPD também"). HTML (Seção 09) e docstring do 2.3.7 atualizados; outputs limpos, precisa rodar.
- (31/08/2026) Removida a palavra "badrate" dos títulos do QUADRO 4 (2.3.7/2.3.8) — o responsável notou a inconsistência: o QUADRO 2 já descreve o conteúdo direto ("qtd x R$ x Δ e contagem"), sem termo genérico, então o QUADRO 4 (que também já é Over ou FPD, sabido pela célula) não devia usar "badrate" — vira só ruído/dúvida de novo ("é over ou badrate?"). Trocado: banner "badrate (asa_v5 x asa_bvs) e contagem por lista vigente" → "asa_v5 x asa_bvs e contagem por lista vigente"; títulos das tabelas `f"{rotulo} — badrate por mês/lista vigente"` → `f"{rotulo} — asa_v5 x asa_bvs por mês/lista vigente"` (mantém o `rotulo` — ex. "Over30M4" — pra não perder a clareza de qual janela). HTML (Seção 09, tabela "qual quadro usa qual fórmula") atualizado igual. "Badrate" como conceito genérico continua explicado na subseção anterior do HTML — só sumiu dos TÍTULOS visíveis no notebook. Outputs limpos de novo.
- (31/08/2026) 2 correções no QUADRO 4 recém-fundido (2.3.7/2.3.8), a pedido do responsável, colando o output puro: (1) ORDEM errada — tabela de contagem (absoluto) vinha antes da de badrate (taxa/%); o responsável apontou que devia ser o contrário ("absoluto primeiro e inadimplência depois" tava ao contrário do padrão), pra ficar consistente com o QUADRO 2 (lá, `qtd x R$ x Δ` — tipo percentual — já vem antes de `contagem` — absoluto). Reordenado: badrate (por mês, por lista vigente) agora vem ANTES da contagem (Geral, Ambos scores) — banner e comentário no código também tiveram a ordem do texto ajustada pra bater. (2) Nome ambíguo — os títulos das 2 tabelas de badrate eram só "Por mês"/"Por lista vigente", sem dizer qual fórmula (o responsável perguntou "estamos considerando over ou badrate aqui?"). Trocado pra incluir o `rotulo` da janela: `f"{rotulo} — badrate por mês"` (ex. "Over30M4 — badrate por mês" no 2.3.7, "FPD15 — badrate por mês" no 2.3.8) — deixa explícito que "badrate" ali É o Over ou o FPD daquela célula, não uma 3ª fórmula (mesmo ponto da correção conceitual anterior, agora refletido no próprio rótulo da tabela, não só na doc). Outputs limpos de novo.
- (31/08/2026) Mesma lógica aplicada de novo, a pedido do responsável: QUADRO 4 (contagem por lista vigente, Geral|Ambos scores) e QUADRO 5 (badrate asa_v5 x asa_bvs, Por mês|Por lista vigente) fundidos num banner só com 4 tabelas lado a lado ("Geral — contagem", "Ambos scores — contagem", "Badrate — por mês", "Badrate — por lista vigente"). Total por janela caiu pra 4 blocos (era 5, era 6 originalmente): Over30M4/FPD15 = banners 1-4, Over60M6/FPD30 = 5-8 — atualizado no docstring da célula 2.3.7 e na tabela "qual quadro usa qual fórmula" do HTML (agora com nota explicando que dentro do QUADRO 4 as 2 primeiras tabelas são checagem de cobertura e as 2 últimas são a separação por score — conceitos diferentes, mesmo lado a lado). Outputs limpos de novo.
- (31/08/2026) Blocos 2.3.7/2.3.8: QUADRO 2 (qtd x R$ x Δ por mês) e QUADRO 3 (contagem por mês) — antes 2 banners/`exibir_lado_a_lado` separados, cada um só com Geral|Ambos scores — foram fundidos num banner só com as 4 tabelas lado a lado (Geral—qtd/R$/Δ, Ambos scores—qtd/R$/Δ, Geral—contagem, Ambos scores—contagem), a pedido do responsável ("acho que dá pra colocar mais coisas lado a lado"). `visualizacao.exibir_lado_a_lado` já suportava N blocos (`flex-wrap`), só precisou trocar o match de cor de `_CORES_LADO_A_LADO` de igualdade exata pra `titulo.startswith(chave)` (títulos agora têm sufixo, tipo "Geral — contagem"). Como usa `contador_bloco` incremental (não número fixo), a fusão renumerou sozinha o resto da sequência: Over30M4/FPD15 = banners 1-5 (era 1-6), Over60M6/FPD30 = 6-10 (era 7-12) — atualizado no docstring da célula 2.3.7 e na tabela "qual quadro usa qual fórmula" do HTML (Seção 09). Outputs das 2 células limpos de novo (precisa rodar).
- (28/08/2026) Correção conceitual na Seção 09 do HTML (a pedido do responsável): "badrate" não é uma fórmula própria/terceira metodologia — é só o nome genérico de "taxa de maus", vale tanto pra Over quanto pra FPD. A seção "Badrate por rating ASA (V5 x BVS)" (tratava isso como método separado, ao lado de FPD/Over) foi renomeada/reescrita pra deixar claro que `calculo.tabela_badrate_v5_bvs_por_periodo()` usa a MESMA fórmula de Over (2.3.7) ou FPD (2.3.8) — só muda a população (asa_v5 x asa_bvs), nunca compara Over com FPD entre si (resposta anterior minha que sugeriu isso como "3ª metodologia" estava errada). Adicionada tabela nova "Qual quadro/gráfico usa qual fórmula" — mapeia os 6 blocos por janela (GRÁFICO/QUADRO 1-6, numeração de `visualizacao.imprimir_banner`) pra qual comparação cada um faz (Geral x Ambos scores, ou asa_v5 x asa_bvs) — só o #6 usa a separação por score, os outros 5 são checagem de cobertura/viés de população.
- (28/08/2026) `visualizacao.imprimir_banner(texto)` (novo) — imprime `texto` dentro de uma moldura de `#`, a pedido do responsável, só pra facilitar referenciar um gráfico/quadro específico numa conversa depois. Aplicado antes de cada gráfico/quadro dos Blocos 2.3.7/2.3.8, com contador `GRÁFICO/QUADRO N` — numeração contínua dentro de cada célula (2.3.7: Over30M4 = 1-6, Over60M6 = 7-12; 2.3.8: reinicia do 1, célula separada). Dessa vez o rewrite do notebook (mesmo contorno via script Node, arquivo grande demais) evitou de propósito qualquer `\n` dentro de f-string (os banners substituíram os antigos `print(f"\n{rotulo}...")`) — não tem mais esse padrão pra repetir o bug anterior.
- (28/08/2026) `visualizacao.exibir_lado_a_lado()`: título de cada bloco ("Geral"/"Ambos scores") ficava alinhado à esquerda em vez de centralizado sobre a tabela — faltava `text-align:center` na div do título (só a tabela em si tinha cabeçalho centralizado via `_centralizar_cabecalho`, não o título do bloco). Corrigido no `src/visualizacao.py` — como o notebook usa `%autoreload 2`, não precisa editar o `.ipynb`, só rodar a célula de novo.
- (28/08/2026) `SyntaxError: unterminated f-string literal` no 2.3.7 — bug introduzido pelo PRÓPRIO script Node usado pra editar o notebook (contorno da Seção acima, arquivo grande demais pra Read/NotebookEdit): escrevi `print(f"\n{rotulo}...")` dentro de um template literal JS (crase), e `\n` dentro de qualquer string JS (inclusive template literal) é interpretado como quebra de linha REAL, não o texto literal `\n` — isso quebrou a f-string Python em 2 linhas físicas (aspas simples não fecham). Corrigido com outro script Node que busca a quebra de linha real seguida de `{rotulo}` (o padrão exato do bug) e substitui pelo backslash-n literal, direto no `.ipynb` já salvo. ⚠️ Lição: ao gerar código Python via string JS (nesse contorno de arquivo grande), qualquer `\n` que deva aparecer LITERAL no Python precisa virar `\\n` no source JS (senão vira newline real na hora que o Node executa o template literal).
- (28/08/2026) A tentativa anterior de "lado a lado" (MultiIndex, `pd.concat(..., keys=[...])` + `_centralizar_e_colorir_grupos`) não resolveu de verdade — mesmo com cabeçalho centralizado, o responsável reportou (colando o output puro de várias tabelas) que continuava sem parecer lado a lado, e citou TAMBÉM pares que nunca foram fundidos em MultiIndex (badrate "por mês" vs "por lista vigente"). Causa raiz: um único `display()` do Jupyter SEMPRE empilha verticalmente em relação ao próximo `display()`, não importa o quão "larga" seja a tabela — precisava ser 1 HTML só com várias tabelas dentro de um flex row. Solução: `visualizacao.exibir_lado_a_lado(*blocos)` (novo) — recebe `(titulo, objeto)` (DataFrame ou Styler), gera `<div style="display:flex">` com uma tabela HTML própria por bloco, cada uma com título e borda colorida (`_CORES_LADO_A_LADO`: "Geral"=azul, "Ambos scores"=verde, resto=cinza neutro). Simplificado `estilizar_tabela_pct_delta`/`estilizar_tabela_grupos` de volta pra tabelas FLAT (sem MultiIndex — cada população agora é uma tabela própria, não uma coluna dentro de uma tabela larga). Aplicado em TODOS os pares do bloco 2.3.7/2.3.8: qtd x R$ x Δ (Geral | Ambos scores), contagem por mês e por lista vigente (Geral | Ambos scores), e badrate (Por mês | Por lista vigente). Outputs das 2 células limpos (código mudou muito, output antigo ficaria enganoso) — precisa rodar de novo.
- (28/08/2026) Cabeçalho de grupo (`Geral`/`Ambos scores`) das tabelas MultiIndex (2.3.7/2.3.8) aparecia encostado à direita em vez de centralizado — sem `text-align` explícito, herdava o alinhamento padrão do notebook, fazendo a tabela parecer "não realmente lado a lado" mesmo estando estruturalmente correta (confirmado inspecionando o HTML gerado via Node, já que o notebook cresceu demais com outputs pra `Read`/`NotebookEdit` conseguirem abrir — editado direto no JSON via script Node). Corrigido com `visualizacao._centralizar_e_colorir_grupos()` (novo helper interno): centraliza todo `<th>` via `set_table_styles` e colore o fundo de cada grupo (`_CORES_GRUPO_COLUNA`: "Geral" azul clarinho, "Ambos scores" verde clarinho, baixa opacidade pra não brigar com tema claro/escuro) tanto no cabeçalho (seletor CSS por classe `th.colN`) quanto nas células (`set_properties`). Usado por `estilizar_tabela_pct_delta()` (já existente) e pela nova `estilizar_tabela_grupos()` (sem formatação de %/Δ — as tabelas de contagem, que são inteiros, agora também passam por isso; antes eram `display()` de `pd.concat()` puro, sem estilo nenhum).
- (28/08/2026) ⚠️ O notebook ficou grande demais (outputs acumulados de várias execuções) pra Read/NotebookEdit abrirem — erro "File content exceeds maximum allowed tokens" mesmo com offset/limit (a ferramenta parece precisar renderizar o arquivo inteiro antes de aplicar o recorte). Contorno usado: editar o `.ipynb` diretamente como JSON via script Node (`fs.readFileSync` + `JSON.parse` + replace no `cell.source` + `JSON.stringify(nb, null, 1) + "\n"` — o notebook usa indentação de 1 espaço por nível, confirmado inspecionando os bytes crus). Se isso voltar a acontecer, mesma abordagem serve; considerar sugerir ao responsável limpar outputs (Restart & Clear Output) se o arquivo continuar crescendo.
- (28/08/2026) Blocos 2.3.7/2.3.8 reformatados (layout, a pedido do responsável): (1) gráficos lado a lado em vez de empilhados — `visualizacao.plot_serie_temporal` já suportava `ax=` pra isso, só precisava usar `plt.subplots(1, 2, ...)` + `plt.tight_layout()` manual (a função só chama `tight_layout()` sozinha quando cria a própria figura). Aplicado tanto na visão "por mês" (qtd | R$) quanto "por lista vigente" (Geral | Ambos scores). (2) Nova tabela `qtd x R$ x Δ` por mês (delta entre os 2 gráficos que já existiam), pra Geral e Ambos scores. (3) Tabelas antes exibidas em 2 `display()` separados (uma pra Geral, outra pra Ambos scores) agora juntas num só DataFrame via `pd.concat([tabela_a, tabela_b], axis=1, keys=["Geral","Ambos scores"])` — gera colunas MultiIndex, aparece como 1 tabela só com os grupos lado a lado. (4) Eixo de período renomeado de `flag_anomes`/`funil_flag_lista_vigente` (nome técnico da coluna de groupby) pra `dt_criacao`/`dt_vigencia_lista` (`.rename_axis(...)`) em TODAS as tabelas indexadas por período do bloco (contagem, qtd/R$/Δ, badrate por score) — não só nas pedidas explicitamente, por consistência. `visualizacao.estilizar_badrate_v5_bvs()` generalizada pra `estilizar_tabela_pct_delta()` (identifica a coluna "Δ" pelo nome, não mais hardcoded pra "asa_v5"/"asa_bvs" — funciona com colunas MultiIndex também, formatando por `subset` de tuplas), reaproveitada na nova tabela qtd/R$/Δ. Mesma estrutura aplicada nos dois blocos (Over e FPD) e nas duas janelas de cada (30m4/60m6, 15/30) — o usuário só pediu explicitamente pro Over, mas mantive os dois blocos estruturalmente iguais (como já eram) em vez de deixá-los divergentes.
- (28/08/2026) Renomeado `df_originado`/`df_elegivel_performance` (nomes confusos, o responsável achou estranho) pra `df_originado_geral`/`df_originado_ambos_scores` — nomenclatura pareada (mesmo prefixo `df_originado_`, só o sufixo muda) deixa explícito que as duas são fatias da MESMA base (quem originou), diferindo só em exigir ou não os 2 scores ASA preenchidos. Confirmado com o responsável: `df_originado_geral` pode incluir quem tem só ASA V5, só ASA BVS, ou nenhum dos dois — a única exigência é `funil_data_pago.notna()`. Aplicado nas variáveis do notebook (2.3.7/2.3.8), nos rótulos de gráfico/print ("Geral"/"Ambos scores") e na Seção 09 do HTML de documentação.
- (28/08/2026) Blocos 2.3.7/2.3.8 reestruturados: o responsável percebeu que os gráficos "por mês"/"por lista vigente" (sem quebra por rating) estavam usando `df_originado_ambos_scores` (exige os 2 scores ASA preenchidos), não representando "o Over/FPD geral" de verdade — era o Over de uma fatia específica (quem tem os 2 scores), sem deixar isso explícito. Corrigido MANTENDO as duas bases lado a lado (não substituindo uma pela outra, a pedido do responsável — serve pra enxergar se há divergência real entre elas): `df_originado_geral` (só `funil_data_pago.notna()`, sem exigir score nenhum) x `df_originado_ambos_scores` (+ `flag_comparacao_asa_v5_bvs == "considerar"`). Visão "por mês": 1 gráfico só com as 2 populações como linhas separadas (`% (qtd)` e `% (R$)`, 2 gráficos). Visão "por lista vigente": 2 gráficos separados (Originado, Elegível) — juntar as duas populações OU as 8 listas no mesmo gráfico ficaria com 16 linhas, optei por manter 8 linhas (só listas) por gráfico e duplicar o gráfico por população. Nova função `calculo.contagem_bons_maus()` (total/bons/maus por período, NÃO em %) impressa logo abaixo de cada visão, pras duas bases — evita interpretar como "sinal real" uma diferença de % que na verdade é ruído de N pequeno. ⚠️ Essa comparação (Originado x Elegível) é DIFERENTE da tabela `asa_v5`/`asa_bvs`/`Δ` que já existia (aquela usa uma população própria POR SCORE, sempre em `df_originado_geral` — não tem "Originado x Elegível" nela).
- (28/08/2026) Nova Seção 09 no HTML "Documentação Técnica" — "Fórmulas de cálculo", a pedido do responsável: FPD, Over, badrate por rating (V5 x BVS, com a explicação de por que as populações são separadas), conversão do funil (quadro-resumo), score contínuo ASA (5.2), classificação em faixas de rating, e migração de rating (as 2 normalizações). Cada fórmula com exemplo numérico pequeno.
- (28/08/2026) `visualizacao.plot_serie_temporal()` ganhou `formato` ("pct" default, comportamento original — anota "%" e formata eixo Y em %; "numero" — anota valor cru, sem % nenhum) — usado no Bloco 2.3.6 pra plotar `asa_v5_media`/`asa_bvs_media` (score 0-1000, não percentual) logo depois de cada tabela impressa de `calculo.resumo_score_asa_v5_bvs`, a pedido do responsável ("manter a tabela, mas facilitar a visualização com um gráfico").
- (28/08/2026) Blocos 2.3.7/2.3.8 (Over/FPD por rating ASA): trocados os 2 gráficos de linha separados (um por `flag_rating_asa_bvs`, outro por `flag_rating_asa_v5`) por 1 heatmap cruzado (rating V5 no eixo X, rating BVS no eixo Y, célula = badrate daquele PAR de ratings) — objetivo era comparar se os dois scores ORDENAM o risco da mesma forma. ⚠️ Substituído logo em seguida (ver próxima decisão) — o heatmap não ficou claro pro responsável.
- (28/08/2026) O heatmap cruzado de badrate (decisão anterior) não ficou claro pro responsável ("não explicou muito bem") — REMOVIDO (`matriz_over_rating_asa`/`matriz_fpd_rating_asa` tiradas do `calculo.py`) e substituído por 2 tabelas simples asa_v5 × asa_bvs × Δ (por mês, por lista vigente), estilizadas (`visualizacao.estilizar_badrate_v5_bvs`: % 1 casa, Δ colorido vermelho/verde). Ponto importante descoberto na conversa: badrate NÃO pode ser calculado com a mesma população pras duas colunas (`flag_comparacao_asa_v5_bvs == "considerar"` exige os dois scores preenchidos ao mesmo tempo — isso deixaria `asa_v5` e `asa_bvs` IDÊNTICOS, Δ sempre 0, já que Over/FPD é propriedade do CONTRATO, não do score usado pra olhar ele). Corrigido calculando cada coluna numa população DIFERENTE: `asa_v5` = badrate de quem tem `flag_rating_asa_v5 != "vazio"`; `asa_bvs` = idem pro BVS — a diferença de COBERTURA de cada score (cada um tem "vazio" em volumes diferentes, ver 2.3.4.1) é o que pode fazer o badrate geral observado diferir, e é isso que o Δ mostra. Nova função `calculo.tabela_badrate_v5_bvs_por_periodo()`, usada em `df_originado_geral` (só `funil_data_pago.notna()`, SEM exigir `flag_comparacao_asa_v5_bvs`) — diferente de `df_originado_ambos_scores` (que continua existindo, usado só nas 2 visões temporais de linha que sobraram).
- (28/08/2026) Etapas 5.2-5.5 implementadas (Blocos 2.3.6-2.3.9), boa parte portando código do acompanhamento do TopUp (`[TopUp]/3 - Analytics/src/{calculo,visualizacao}.py`), a pedido do responsável:
  - **5.2** (`calculo.resumo_score_asa_v5_bvs`, novo): média/mediana/desvio do score CONTÍNUO (`funil_asa_v5`/`asa_asa_bvs_score`) e do delta, por mês e por lista vigente — só quem é `flag_comparacao_asa_v5_bvs == "considerar"`. Complementa a matriz de rating (2.3.5, categórica) olhando o valor em si.
  - **5.3/5.4** (`calculo.serie_over`/`serie_fpd` + `visualizacao.plot_serie_temporal`, portados do TopUp): mesmas fórmulas oficiais (Over% (qtd)=sum(overXmY)/count(contratos); % (R$)=sum(overXmY_val)/sum(valor_originado_bruto); FPD% (qtd)=sum(fpd_x)/sum(elegivel_fpd_x); % (R$) análogo), só trocando as colunas sem prefixo do `df_contratos` do TopUp pelas prefixadas do super base (`contratos_fpd_x`/`contratos_overXmY`) e `loan_id`→`cpf` (não existe `loan_id` no super base; 1 linha = 1 CPF já dedup). Rodados em `df_originado_ambos_scores` = `df_super_base_funil` filtrado a `funil_data_pago.notna() & flag_comparacao_asa_v5_bvs=="considerar"` (só quem originou E tem os 2 scores) — Over30M4/Over60M6 (2.3.7) e FPD15/FPD30 (2.3.8), cada um em 4 visões (mês sem quebra — % qtd vs % R$; lista vigente/rating BVS/rating V5 — só % qtd, mesmo padrão do TopUp de quebrar por categoria só com a métrica de contagem). 2.3.8 depende de `df_originado_ambos_scores`/`visoes_performance` definidos no 2.3.7 rodar antes.
  - **5.5** (`calculo.quadro_resumo_funil` + `formatar_quadro_resumo`, portados verbatim do TopUp): quadro Leads→PQ→Aprovados→Pagos por mês, fórmula oficial QuickSight — roda em `df_funil` BRUTO (não no super base; usa `core_lead_id`/`data_processamento`, que existem no funil bruto — mesma tabela Redshift do TopUp, `pol_ae_sandbox.base_funil_analytics` — mas não foram trazidos pra `config.COLUNAS_SUPER_BASE_FUNIL`).
  - **5.6** (KS/Gini) — responsável ainda não definiu a abordagem ("não pensei em como usar isso"); `calculo.ks_gini()` já existe no arquivo (criado no boilerplate inicial do projeto), não confirmado ainda se serve como está.
- (28/08/2026) Bug no `eixo_total="coluna"` (recém-criado): `sharey=ax_main` ao criar a coluna "Total" apagava os rótulos do eixo Y do heatmap PRINCIPAL (ficava em branco — visto pelo responsável rodando a célula). Corrigido tirando o `sharey` — as duas heatmaps (principal e coluna Total) já têm o mesmo nº de linhas (mesmo `pivot`), então o alinhamento vertical acontece sozinho, sem precisar compartilhar o eixo.
- (28/08/2026) `visualizacao.plot_heatmap_flag_por_anomes()` ganhou `eixo_total` ("linha" default, soma por coluna via `_desenhar_total`; "coluna", soma por linha via `_desenhar_total_coluna` nova) — a visão "por destino" da matriz de migração (normalizada por linha) estava sem nenhum Total (removido antes por não bater com o Total de baixo, que soma por coluna); agora tem Total de verdade, só que como COLUNA à direita do heatmap (`sharey` com o eixo principal, mesma paleta neutra `COR_TOTAL` do Total de baixo).
- (28/08/2026) Matriz de migração (2.3.5) ganhou 2 visões — bug de leitura identificado pelo responsável: somar uma LINHA (ex. rating BVS = A2) não dava 100%, porque a normalização original era por COLUNA (rating V5/origem) — pergunta "de quem tinha X no V5, foi pra onde no BVS", não "esse A2 do BVS hoje, era o quê antes". `tratamento._distribuir_flag()` ganhou `normalizar_por` ("eixo_x" default, ou "categoria" pra normalizar por linha); `matriz_migracao_rating_asa()` expõe isso como `normalizar_por="origem"` (default, cada coluna soma 100%) ou `"destino"` (cada linha soma 100% — responde a pergunta do responsável). Notebook agora mostra as duas visões lado a lado; a de "destino" não tem linha Total (ela soma por coluna, sem sentido quando a normalização já é por linha).
- (28/08/2026) `visualizacao._desenhar_heatmap()`/`plot_heatmap_flag_por_anomes()` ganharam `destacar_diagonal` (a pedido do responsável): contorna em vermelho toda célula onde o rótulo da linha bate com o da coluna — comparando por RÓTULO, não por posição, então funciona mesmo se os dois eixos não tiverem exatamente as mesmas categorias/ordem. Só faz sentido quando os dois eixos são a mesma categoria (matriz de migração V5×BVS — destaca "sem mudança de rating"); não é o default, não afeta os heatmaps de faixa de score x lista/mês. Aplicado na matriz do 2.3.5.
- (28/08/2026) Refino no heatmap de migração (2.3.5), a pedido do responsável: (1) título ficava confuso com 3 rótulos quase iguais (`titulo` auto-gerado repetia o texto do `ylabel`, ao lado do próprio `ylabel`/`xlabel` nos eixos) — corrigido passando `titulo="Migração de rating: ASA V5 → ASA BVS"` explícito em vez de deixar o default. (2) Ordem trocada de pior→melhor (`nao_trabalhavel`→`AA`) pra melhor→pior (`AA`→`nao_trabalhavel`, com `">1000"` acima de `AA` por completude) nos dois eixos — mais intuitivo pra leitura de rating.
- (27/08/2026) Novo Bloco 2.3.5 (`flag_comparacao_asa_v5_bvs` + matriz de migração de rating): `tratamento.flag_comparacao_rating_asa()` compara `flag_rating_asa_v5`/`flag_rating_asa_bvs` só pela PRESENÇA de `"vazio"` em cada um (não pelo valor do rating) — `"ambos_vazios"`, `"somente_um_vazio"` ou `"considerar"` (os dois têm score, seja rating de verdade ou `"nao_trabalhavel"`/`">1000"` — só `"vazio"` conta como ausência). `tratamento.matriz_migracao_rating_asa()` usa só quem é `"considerar"` pra montar a matriz rating V5 (origem) x rating BVS (destino), via o mesmo `_distribuir_flag`/`plot_heatmap_flag_por_anomes` já usado no 2.3.4.1 — % é por coluna (rating V5), então cada coluna mostra "de quem tinha esse rating no V5, pra onde foi no BVS". Objetivo: avaliar a migração do ASA V5 (já em produção) pro ASA BVS (novo). Nota de robustez: a lista de ordem (`ordem_rating`/`ordem_categorias`) só mostra categorias explicitamente listadas — qualquer categoria fora da lista passada é SILENCIOSAMENTE excluída do heatmap (via `pivot.reindex`), não gera erro nem aviso. O `ordem_rating` do 2.3.4.1 não incluía `">1000"`; sem impacto até agora porque nenhuma linha caiu nessa faixa (conferido pela soma do `value_counts` batendo com o total), mas vale lembrar se um dia aparecer.
- (27/08/2026) Bloco 2.3.5 ganhou uma 2ª visão da mesma validação (a pedido do responsável, consolidada num único bloco/célula em vez de virar um "2.3.6" separado — "pra não ficar um monte de subblocos pra mesma coisa"): eixo X = `funil_flag_lista_vigente` em vez de mês (`tratamento.distribuicao_flag_por_lista_vigente()`). Motivo: agrupar por mês "suja" a leitura quando um mês tem dias em `config.SEM_LISTA_VIGENTE` E dias já numa lista real (ex. novembro/2025 tem 1-26 sem lista e 27-30 na lista `2025-11-26`, tudo na mesma coluna "2025-11"); agrupando pela lista vigente em si, cada uma vira sua própria coluna, sem essa mistura. Refatorado pra reaproveitar: `tratamento._distribuir_flag()` virou o helper compartilhado por `distribuicao_flag_por_anomes` (mês) e `distribuicao_flag_por_lista_vigente` (lista) — coluna de saída renomeada de `anomes` pra `eixo_x` (genérica, serve pros dois). `visualizacao.plot_heatmap_flag_por_anomes()` ganhou `xlabel`/`ordem_colunas` (a sentinela `config.SEM_LISTA_VIGENTE` precisa de ordem explícita — como string começa com "a", cairia no FIM da ordem alfabética das listas reais "2025-...", não no início cronológico).
- (27/08/2026) Novo Bloco 2.3.5 (`tratamento.distribuicao_flag_por_anomes()` + `visualizacao.plot_heatmap_flag_por_anomes()`) — heatmap categoria (`flag_rating_asa_bvs`, ordem vazio/nao_trabalhavel + E→AA) x mês de `funil_data_criacao`, % dentro de cada mês + linha Total, pra validar se o volume de `"vazio"` (618k linhas, ver Próximos Passos) concentra em algum período. Reaproveita a infra de heatmap já existente: `visualizacao._desenhar_heatmap`/`_desenhar_total` ganharam parâmetros opcionais (`ylabel`, `xlabel`, `rotulos`) com default = comportamento antigo (faixa de score/lista vigente), pra servir também a um índice categórico arbitrário sem duplicar o layout de GridSpec — chamadas existentes (`plot_heatmap_score_por_lista`/`plot_heatmaps_score_por_lista`) não mudam.
- (27/08/2026) `tratamento.classificar_rating_asa()` ganhou uma 3ª marcação explícita: score nulo na origem (`asa_asa_bvs_score`/`funil_asa_v5`) agora vira `config.SEM_SCORE_ASA` (`"vazio"`) em vez de `NaN` — mesmo motivo/padrão de `config.SEM_LISTA_VIGENTE` (a pedido do responsável, ao ver ~618k/~1,28MM `NaN` em `flag_rating_asa_bvs`/`flag_rating_asa_v5`). Com isso as 3 marcações da função ficam: `">1000"` (acima do teto), `"nao_trabalhavel"` (< 276), `"vazio"` (score nulo) — nenhuma delas é `NaN`.
- (27/08/2026) `flag_lista_vigente` deixou de usar `None`/NaN pro lead criado antes da vigência da 1ª lista — virou `config.SEM_LISTA_VIGENTE = "anterior_1a_lista"` (a pedido do responsável: "None parece que veio vazio após um cruzamento", quando na verdade é limitação real de cobertura histórica, não falha de JOIN). Atualizados os 3 pontos que dependiam de `None`/NaN pra esse sentido: `tratamento.atribuir_lista_vigente()` (inicializa com a constante, conta com `==` em vez de `.isna()`), `tratamento.cpfs_repetidos_entre_listas()` e `extracao.busca_por_cpfs_vigentes()` (ambos filtravam com `dropna()`, agora comparam com a constante — a coluna não fica mais nula). `_preparar_fonte_super_base()` ganhou `errors="coerce"` no `pd.to_datetime(col_lista_origem)` pra essa string virar NaT de novo ao derivar a chave `lista` do merge (mesmo efeito de antes — linha sem vigência não casa com bvs/asa_bvs — só muda a coluna visível). `extracao.busca_por_cpfs_vigentes()` também passou a extrair só as 2 colunas usadas (cpf+lista) ANTES de filtrar/agrupar, em vez de fazer isso na tabela larga do funil (mesmo padrão de `MemoryError` já corrigido alhures).
- (27/08/2026) Novo Bloco 2.3.4 no notebook: `tratamento.atribuir_flags_rating_asa(df_super_base_funil)` cria `flag_rating_asa_bvs` (a partir de `asa_asa_bvs_score`) e `flag_rating_asa_v5` (a partir de `funil_asa_v5`), classificando os dois na MESMA tabela de faixas (`config.FAIXAS_RATING_ASA`: E 276-370, D 371-450, C 451-530, B 531-660, A2 661-750, A1 751-880, AA 881-1000 — tabela fornecida pelo responsável) via `tratamento.classificar_rating_asa()`. Fora da tabela não vira NaN (a pedido do responsável): score > 1000 → `">1000"`; score < 276 → `"nao_trabalhavel"`. Só fica nulo o score que já era nulo na origem (comparação com NaN nunca é True, então essas linhas não são sobrescritas). Serve pra comparar o rating do ASA BVS (novo) com o do ASA V5 (já em produção, hoje espelhado em `funil_cluster_asa_v5`) no mesmo critério.
- (26/08/2026) A correção anterior (`df[colunas].copy()` → `pd.DataFrame({c: df[c] for c in colunas})`) reduziu mas não eliminou o `MemoryError` em `_preparar_fonte_super_base` — chegou a quebrar em só **136 MiB** pra (6, ~2,97MM), mostrando o quanto essa sessão está com pouca memória contígua livre (fragmentação, não falta de RAM total). Causa: o construtor `pd.DataFrame({...})` continua consolidando colunas de mesmo dtype num bloco só por padrão (`copy=True` → `consolidate=True` internamente em `dict_to_mgr`/`arrays_to_mgr`), então o `np.empty` grande acontecia DENTRO do próprio `pd.DataFrame(...)`, não mais no `df[colunas]` de antes. Corrigido com `pd.DataFrame({...}, copy=False)` — evita a consolidação, cada coluna fica como bloco separado, nunca precisa de um array 2D grande contíguo. Aplicado nos 3 pontos que já tinham o padrão "extrair colunas num dict" (`_preparar_fonte_super_base` x2, `cpfs_repetidos_entre_listas`). ⚠️ Se o `.merge()` em `montar_super_base_funil` também estourar (ainda não testado), a causa provável é a mesma — merge pode reconsolidar o resultado.
- (26/08/2026) Mesmo bug apareceu numa 3ª função: `cpfs_repetidos_entre_listas` fazia `df_funil_tagueado.dropna(subset=[col_lista])` — filtrar linhas (boolean mask) na tabela LARGA inteira (65 colunas `object`) força o pandas a montar um bloco novo com todas elas de uma vez (pediu 1.32 GiB pra (65, 2.724.661) e estourou), mesmo sendo só uma checagem read-only que só usa 2 colunas. Corrigido cortando pra `[cpf, flag_lista_vigente]` ANTES do `dropna`, mesmo padrão de `_preparar_fonte_super_base`. **Regra geral daqui pra frente**: qualquer função que só precisa de um subconjunto pequeno de colunas de `df_funil_tagueado` (65+ colunas `object`) deve extrair essas colunas uma a uma (`pd.DataFrame({c: df[c] for c in cols})`) ANTES de filtrar linhas (`dropna`/boolean mask/`.loc`/`drop_duplicates`) — filtrar linhas na tabela larga inteira é o gatilho do `MemoryError`, não o tamanho final do resultado.
- (26/08/2026) `_preparar_fonte_super_base` continuou dando `MemoryError` mesmo depois de cortar as colunas ANTES do dedup — o corte em si (`df[lista_de_colunas]`) já era a causa: selecionar VÁRIAS colunas `object` de uma vez faz o pandas montar um bloco novo com um `take` só (pediu 793 MiB pra uma seleção de 35 colunas x ~2,97MM linhas, e mesmo esse tamanho menor ainda estourava por fragmentação). Corrigido trocando `df[colunas].copy()` por `pd.DataFrame({c: df[c] for c in colunas})` — seleciona uma coluna por vez (cada extração é bem menor, ~1/35 do tamanho), evitando precisar de um bloco contíguo grande numa única alocação. Aplicado nos dois pontos da função (corte inicial e corte final pós-dedup).
- (26/08/2026) `_preparar_fonte_super_base` deu `MemoryError` (mesma família de bug de fragmentação por consolidação de colunas `object` — já visto em `_carregar_tabela_em_lotes`) ao processar o funil inteiro (~90 colunas brutas, 67 `object`) com ~2,5MM linhas após a ampliação pra 8 listas. Causa: cortava pras ~54 colunas curadas DEPOIS do sort/dedup/`reset_index`, então a operação cara rodava na tabela larga. Corrigido cortando as colunas necessárias ANTES do dedup, e removendo o `reset_index(drop=True)` (cosmético — merge não liga pro índice, só forçava mais uma cópia inteira).
- (26/08/2026) `config.LISTAS` ampliado de 5 pra 8 datas — agora cobre TODAS as rodadas onde credit_bvs_papn E asa_bvs_lista_papn têm linha (2025-11-26, 12-23, 2026-01-26, 02-26, 04-01, 05-11, 07-06, 07-29). `credit_bvs_papn` sozinha tem 24 rodadas confirmadas (desde 2024-09-23) — as que ficaram de fora (inclusive `2025-10-27`, que estava no corte antigo) não têm linha em `asa_bvs_lista_papn` (tabela completa das 24 + vigência calculada está no HTML de documentação, Seção 05). `config.DATA_REF_VIGENCIA` atualizado pra `"2026-08-26"` (hoje) — `2026-07-29` agora é genuinamente a rodada mais recente confirmada, não precisa mais fechar a janela numa data arbitrária mais cedo.
- (26/08/2026) `config.vigencias()` fechava a janela da ÚLTIMA lista em `date.today()` quando não recebia `data_ref` — como `config.LISTAS` foi cortado de propósito (analisar período mais antigo), a última lista (2026-02-26) virou uma janela de ~6 meses (até hoje, 26/08/2026) em vez de ~1 mês, absorvendo funil de meses depois mesmo sabendo que na vida real já existia outra rodada (2026-04-01, confirmado no dump de dt_created do credit_bvs_papn). Causou o volume desproporcional (1,15MM vs ~110-190k nas outras) que o responsável identificou comparando `funil_data_criacao` min/max por lista. Corrigido com `config.DATA_REF_VIGENCIA = "2026-04-01"` como default de `vigencias()`, substituindo `date.today()`. ⚠️ Cache de `busca_por_cpfs_vigentes` não é invalidado por essa mudança (a chave é o SET de labels de lista, que não mudou) — precisa `usar_cache=False` uma vez pra pegar o CPF set corrigido.
- `montar_super_base_funil` agora tipa `funil_data_criacao`/`funil_data_pago`/`bvs_dt_created`/`asa_created_at` como datetime (`pd.to_datetime`, antes ficavam object/string) e o resumo por lista ganhou `data_criacao_min`/`data_criacao_max` — dá pra conferir visualmente o tamanho real de cada janela de vigência.
- (26/08/2026) Bug real (não só distorção de volume) em `montar_super_base_funil`: o merge com df_bvs_funil/df_asa_bvs_funil era só por `cpf`, e o dedup dessas fontes era global por CPF (mantinha só a linha da lista mais recente daquele CPF). Resultado: se um CPF casou em duas listas diferentes, a linha de BVS de UMA lista grudava em TODAS as linhas do funil daquele CPF — inclusive as de outra lista. Corrigido (a pedido do responsável) pra dedup por `(cpf, lista)` em cada fonte (`_preparar_fonte_super_base` ganhou `col_lista_origem`) e merge por `["cpf", "lista"]` — cada linha do funil só casa com o dado da SUA própria rodada. `df_contratos` continua casando só por `cpf` (não tem conceito de lista). Granularidade do super base mudou de "1 linha por CPF" pra "1 linha por (CPF, lista vigente)".
- (26/08/2026) `busca_por_cpfs_vigentes()` comparava `t.cpf` (funil, sempre zero-preenchido a 11 dígitos) direto contra `b.{coluna_cpf}` (coluna original na tabela BVS/ASA BVS, formato desconhecido — pode ser NUMERIC sem zero à esquerda, ou VARCHAR sem padding) — CPFs começando com "0" (~10%) podiam não casar, e sumiam em silêncio (nunca chegavam a voltar do JOIN pra corrigir depois em Python). Corrigido com `LPAD(REGEXP_REPLACE(CAST(b.{coluna_cpf} AS VARCHAR), '[^0-9]', ''), 11, '0')` no lado da tabela — mesmo padrão já usado no projeto "Cascata PA BVS" pra `credit_bvs.credit_bvs_papn` (`CPF_LISTA` em config.py de lá). ⚠️ Cache de df_bvs_funil/df_asa_bvs_funil salvo ANTES dessa correção está com o resultado antigo — precisa rodar `busca_por_cpfs_vigentes(..., usar_cache=False)` uma vez pra regenerar.
- (26/08/2026) Bloco 3 novo — `df_super_base_funil` (`tratamento.montar_super_base_funil`): 1 linha por CPF do funil, juntando funil + df_bvs_funil + df_asa_bvs_funil + df_contratos (opcional, enriquece com FPD/Over só para quem originou), cada um com um recorte curado de colunas (`config.COLUNAS_SUPER_BASE_{FUNIL,BVS,ASA_BVS,CONTRATOS}` — colunas do funil e contratos definidas pelo responsável, não por mim; ajustar lá se faltar coluna) e prefixo por origem (`funil_`/`bvs_`/`asa_`/`contratos_`, `config.PREFIXO_*`) em tudo que não for `cpf`. Dedup por CPF usando o registro mais recente (`data_criacao`/`dt_created`/`created_at`), reaproveitando `tratamento.dedup_por_cpf`.
- (26/08/2026) Causa raiz real do `TypeError` no heatmap "absoluto": `distribuicao_score_asa_bvs_por_lista()` inclui `2025-10-27`, que tem 0 linhas em `asa_bvs_lista_papn` — o DataFrame vazio resultante fica com `dtype=object` (sem dado pra inferir tipo), e isso contamina a coluna `volume` inteira pra object depois do `pd.concat` com as outras 4 listas (que têm dado normal). `pct` não quebrava por ser recalculada do zero em pandas (sempre float64). Corrigido com `pd.to_numeric(...)` explícito nas 3 colunas (`faixa_score`, `volume`, `pct`) logo após o concat — mais robusto que confiar em `coagir_numericos` sozinho, que só converte coluna com pelo menos 1 valor não-nulo.
- Notebook reorganizado (usuário): Bloco 1.3 quebrado em subitens 1.3.1-1.3.5 (uma etapa de carga por célula); "Bloco 3 — EDA" renomeado para "Bloco 2 — EDA", subitens renumerados 2.1-2.3 pra bater.
- (26/08/2026) `_carregar_tabela_em_lotes`/`load_particao_por_created_at`/`load_asa_bvs_amostra` quebravam no meio de cargas longas (`ValueError: Table schema does not match schema used to create file` — visto após 19min de df_funil) porque o pyarrow infere `timestamp[ns]` vs `timestamp[us]` (ou outra diferença sutil) dependendo do conteúdo de cada lote (ex. lote com coluna de data toda nula infere unidade diferente de um lote com valores reais). Corrigido com `tabela_arrow.cast(escritor.schema)` antes de cada `write_table` (exceto o primeiro lote, que define o schema).
- (25/08/2026) Confirmado via `GROUP BY created_at` que `asa_bvs_lista_papn` só tem essas rodadas: 2025-11-26, 12-23, 2026-01-26, 02-26, 04-01, 05-11, 07-06, 07-29 — **não tem 2025-10-27**. `checar_disponibilidade_listas()` deve pegar isso quando rodada com as 5 datas de `config.LISTAS`.
- `busca_por_cpfs_vigentes()` não pegava listas de `config.LISTAS` sem nenhum CPF do funil naquela janela (esperado — groupby só itera categoria com linha) — antes sumia em silêncio, agora avisa explicitamente comparando com `config.LISTAS` e sugere conferir `df_funil['data_criacao'].min()`.
- `busca_por_cpfs_vigentes()` ganhou cache em parquet por fonte (`df_bvs_funil__<hash>` / `df_asa_bvs_funil__<hash>`, hash das listas presentes) — antes recarregava tudo via Redshift a cada execução do Bloco 1.3.
- Bug de tipo: `COUNT(*)`/expressões numéricas do Redshift às vezes voltam como coluna `dtype=object` contendo `int`/`Decimal` puro do Python (não `numpy.int64` nativo) — `conexao.query()` só convertia `Decimal`, não pegava isso, e quebrou o heatmap absoluto (`TypeError: Image data of dtype object`). Generalizado para `conexao.coagir_numericos()` (baseado em tipo, nunca mexe em coluna string mesmo que "pareça" número, ex. CPF) — aplicado em `query()` e em todo DataFrame montado via `cursor.fetchmany()` (`load_particao_por_created_at`, `_carregar_tabela_em_lotes`, `load_asa_bvs_amostra`, `busca_por_cpfs_vigentes`).
- Estrutura do projeto (`3 - Analytics/src/{bootstrap,config,conexao,extracao,tratamento,calculo,visualizacao}.py` + notebook `0 - Acomp ASA BVS [AE].ipynb`) replicada do padrão usado no acompanhamento do TopUp, mas com `conexao.py`/`extracao.py` adaptados para ler direto do S3 (BVS é landing zone S3, não Redshift).
- Pipeline atual da Lista PA BVS (`bvs-batch-processing`, PySpark/EMR): join por CPF → segmentação (16 segmentos) → motor de 25 regras PA/PN → grau de assertividade A–J → limite → swap tracking. Nenhuma referência ao ASA BVS existe hoje no código — é uma inclusão nova.
- Cortes de score BVS hoje aplicados (baseline para comparação): 325 (Quitado/Returning/Simulação/Minuto), 350 (Refin do Fin/Top Up); `bhv_score_2_1` com corte 188 para Refin do Fin/Top Up.
- Output final passa por `dynamo_output` (20 colunas fixas, consumido pelo Crivo) — incluir o ASA BVS como nova coluna exige coordenação com o time Crivo/Tech antes de ir para produção.
- `credit_bvs.credit_bvs_papn` (df_bvs) é tabela Hudi/Spectrum com pruning por partição `dt_created`. `temp_tests_bi.asa_bvs_lista_papn` (df_asa_bvs) é tabela Redshift comum, SEM sort/dist key em `created_at` — filtrar por ela exige scan da tabela inteira. **Correção (25/08/2026):** o `EXPLAIN` sugeria df_bvs pequeno (~3,2MM linhas), mas isso é só a estimativa aproximada do planner pra tabelas Spectrum/Hudi — confirmado via `GROUP BY dt_created` direto na tabela que df_bvs tem **~25 a 30MM linhas por `dt_created`, mesma escala de df_asa_bvs**. `load_bvs()` teve o padrão de `retornar_df` trocado pra `False` por causa disso (evitar o mesmo OOM que já aconteceu com df_asa_bvs).
- Carregar df_asa_bvs por inteiro (mesmo em lotes) causou OOM na janela do editor — a causa era reler o parquet gravado de volta pra RAM no final (`pd.read_parquet` do arquivo completo). Corrigido com `retornar_df=False` (devolve só o caminho do arquivo).
- Decisão de arquitetura (24/08/2026): não carregar df_asa_bvs por inteiro para uso corrente — o funil tem só ~300-400k linhas/mês, então filtramos pelo funil primeiro. Cada lead do funil é marcado com a lista BVS/ASA BVS **vigente** na sua `data_criacao` (`tratamento.atribuir_lista_vigente`, regra emprestada do projeto "Cascata PA BVS": lista vale do dia seguinte à sua data até a véspera da próxima — `config.LISTAS`/`config.vigencias()`), e a busca é um JOIN por CPF direto no Redshift via temp table (`extracao.busca_por_cpfs_vigentes`), trazendo só o que casou com o funil.
- `config.LISTAS` trocado (25/08/2026) para período mais antigo — 2025-10-27, 11-26, 12-23, 2026-01-26, 02-26 — para dar maturação de FPD/Over. Confirmado via `GROUP BY dt_created` em credit_bvs_papn que essas 5 datas existem lá, sem lacunas entre elas nesse período (diferente do trecho mais recente, abr-jul/2026, que tem datas extras não previstas: 2026-04-17 e 2026-07-01).
- 2026-04-28 e 2026-06-03 (datas antigas de LISTAS, já trocadas) tinham 0 linhas em `temp_tests_bi.asa_bvs_lista_papn` — as duas tabelas parecem rodar em cadências de data DIFERENTES. Criada `extracao.checar_disponibilidade_listas()` pra conferir isso antes de confiar numa data nova.
- Distribuição do score ASA BVS na base ESCORADA INTEIRA (não só quem entrou no funil) é feita via `GROUP BY` agregado direto no Redshift (`extracao.distribuicao_score_asa_bvs`) — não precisa baixar as ~30MM linhas, só o resumo por faixa de 100 pontos.
- Cache de df_contratos/df_funil revalidava por data (`max(data_criacao) >= hoje - D`), o que recarregava tudo de novo sempre que a fonte atrasa mais que `D` dias — mesmo sem nada de novo (visto: cache e Redshift com a mesma contagem, recarregou do zero assim mesmo). Trocado por comparação de contagem de linhas (cache local via metadado do parquet x `COUNT(*)` remoto) — mais barato e sem essa suposição. `config.DIAS_DEFASAGEM_REDSHIFT` removido (não é mais usado).
- `load_contratos()`/`load_funil()` também sofriam `MemoryError` (mesmo tabelas "pequenas", ex. df_funil ~2,4MM linhas x 91 colunas) — `conexao.query()` usa `cursor.fetch_dataframe()`, que monta um array único de uma vez (fragmentação de memória no Windows). Trocado por `extracao._carregar_tabela_em_lotes()` (mesmo padrão de lotes/parquet incremental/retry usado em df_bvs/df_asa_bvs) — resolve o erro e dá print de progresso (contagem prévia + tqdm).
- Movida a busca por CPFs vigentes (`atribuir_lista_vigente` + `busca_por_cpfs_vigentes`) do Bloco 4 pro Bloco 1.3 — o load antigo de df_bvs/df_asa_bvs completos (sem filtro de CPF) no Bloco 1.3 estava estourando. Bloco 1.3 agora só carrega df_contratos/df_funil e já busca df_bvs_funil/df_asa_bvs_funil (filtrados pelo funil). Bloco 3.4 (EDA) faz sua própria carga isolada de `df_bvs_eda` (população completa) quando precisar dela pontualmente.

## Anomalias Encontradas
<!-- Ex: "Safra Mai/24 apresentou spike de FPD fora do padrão — investigar outlier" -->

## Próximos Passos
- (28/08/2026) Etapa 5 (Avaliação de performance do ASA BVS) detalhada em 6 subetapas pelo responsável — status em `ATA.html`, mas registrado aqui pra não se perder entre sessões:
  - 5.1 Matriz cruzada ASA V5 x ASA BVS — ✅ concluída (Bloco 2.3.5 do notebook, `flag_comparacao_asa_v5_bvs` + `matriz_migracao_rating_asa`).
  - 5.2 Avaliar diferença do número entre ASA V5 e ASA BVS — pendente (score contínuo, não só faixa/rating — ex. `asa_asa_bvs_score - funil_asa_v5`).
  - 5.3 Badrate/inadimplência (Overs) — pendente (`contratos_over30m4/60m6/90m9/90m12`, já em `df_super_base_funil`).
  - 5.4 FPD's — pendente (`contratos_fpd_1/3/5/7/15/30/45/60/90`, já em `df_super_base_funil`).
  - 5.5 Conversões — pendente (taxa de originação por faixa de rating).
  - 5.6 KS/Gini — pendente (objetivo central da etapa).
- (27/08/2026) Investigar o volume de `"vazio"` em `flag_rating_asa_bvs` (618,349 linhas) e `flag_rating_asa_v5` (1.280.264 linhas) no `df_super_base_funil` — comportamento atual é intencional (score de origem, `asa_asa_bvs_score`/`funil_asa_v5`, já era nulo; ver `tratamento.classificar_rating_asa`), mas o volume é grande o bastante pra valer entender ANTES da Etapa 5 (KS/Gini): concentra em algum canal/segmento/período específico? É esperado (CPF sem match na rodada, ver `flag_match`) ou aponta falha de cobertura em algum recorte?
- Rodar `extracao.checar_disponibilidade_listas()` com as novas 5 datas de `config.LISTAS` pra confirmar que `asa_bvs_lista_papn` também tem linhas nelas (só validamos isso em credit_bvs_papn até agora).
- Definir a coluna de data de referência para revalidação de cache de df_bvs/df_asa_bvs (assumido "data_criacao" como placeholder — confirmar no schema real).
- Definir KPI esperado e hipótese principal (ambos "a definir" no briefing).
- Coordenar com Crivo/Tech a viabilidade de incluir novo score no `dynamo_output`.

## Histórico de Alterações
| Data | Decisão | Por quê |
|------|---------|---------|
| 24/08/2026 | Estrutura de projeto criada seguindo padrão TopUp | Reuso de convenção já validada pelo responsável em outro acompanhamento |
| 24/08/2026 | `conexao.py`/`extracao.py` usam S3 (boto3) como fonte principal, com `query()` Redshift mantido como utilitário | BVS é landing zone S3; Redshift só é usado para bases complementares (ex. contratos/Crivo) |
| 24/08/2026 | Bloco 1 do notebook carrega df_contratos (`pol_ae_sandbox.base_contratos_analytics`), df_funil (`pol_ae_sandbox.base_funil_analytics`), df_bvs (`credit_bvs.credit_bvs_papn`) e df_asa_bvs (`temp_tests_bi.asa_bvs_lista_papn`), todos via Redshift com cache parquet D-2 | Responsável confirmou reuso do mesmo padrão do TopUp; ASA BVS já está calculado em tabela temp, não precisa ser derivado |
